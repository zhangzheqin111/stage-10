# BloomBeat 开发日志 - 2026-06-27

## 今日完成

- 继续 Stage 8 浏览器 UI 上传验收，不开启新阶段。
- 用户使用重新生成的 `stage8-validation-assets/stage8-ui-background.png` 后，确认新图可以显示。
- 验证用户生成的云端链接：`http://127.0.0.1:3000/gift/gift-mqv4mbwa-014402fb`。
- 读取 `/api/gifts/gift-mqv4mbwa-014402fb`，确认云端礼物包含 Supabase `backgroundImageUrl`，礼物页 `/gift/gift-mqv4mbwa-014402fb` 返回 200。
- 定位并修复真实 UI 流程中上传音频 `audioUrl` 丢失的问题：内容页原先只读取 localStorage 草稿，后续保存图片/祝福时会覆盖 IndexedDB 里的完整音频 data URL。
- `/create/content` 现在会在初始加载后合并 IndexedDB 中的完整草稿，恢复 localStorage 因 4MB 安全线被移除的 `audioUrl` 和 `backgroundImageUrl`，避免内容页编辑覆盖上传音频。
- 用户重新生成云端链接 `http://127.0.0.1:3000/gift/gift-mqv7kqc8-10a79722`，已确认云端礼物同时包含 Supabase `audioUrl` 与 `backgroundImageUrl`。

## 今日验证

- 图片上传链路验收通过：新背景图能显示，并且生成的云端礼物保存了 Supabase 图片 URL。
- 云端礼物页探活通过：`/gift/gift-mqv4mbwa-014402fb` 返回 200。
- 当前发现新的 Stage 8 验收问题：该礼物 `songSourceType` 为 `upload`，`songTitle` 是上传音频文件名，但云端礼物 JSON 中缺少 `audioUrl` 字段。
- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 修复后新链接 `/gift/gift-mqv7kqc8-10a79722` 验收通过：
  - `/api/gifts/gift-mqv7kqc8-10a79722` 返回 `audioUrl`，且为 Supabase Storage mp3 URL。
  - `/api/gifts/gift-mqv7kqc8-10a79722` 返回 `backgroundImageUrl`，且为 Supabase Storage jpg URL。
  - `/gift/gift-mqv7kqc8-10a79722` 返回 200。
  - 音频对象 HEAD 返回 200，Content-Type 为 `audio/mpeg`，大小约 7.5MB，低于 10MB 限制。
  - 图片对象 HEAD 返回 200，Content-Type 为 `image/jpeg`，大小约 178KB，低于 8MB 限制。

## 待办事项

- 使用另一浏览器或移动设备打开 `/gift/gift-mqv7kqc8-10a79722`，确认背景图、标题、祝福、主题和上传音频播放入口一致。

## 风险 / 阻塞

- 上传音频云端持久化已通过，但移动端浏览器可能仍受自动播放策略影响，需要通过点击“音量”入口判断是否能播放。
- 当前本地链接是 `127.0.0.1`，只能在本机打开；跨设备验收仍需要 HTTPS 部署或隧道。

## 下一步计划

- 进入 Stage 8 跨浏览器 / 手机端验收：优先验证桌面另一个浏览器，再验证 HTTPS 环境下的 iPhone Safari、Android Chrome 和微信内置浏览器。

---

## Stage 8 HTTPS 隧道验收入口今日完成

- 继续 Stage 8 跨设备 / 手机端验收准备，不开启新阶段。
- 已确认本地服务 `http://127.0.0.1:3000/` 可访问。
- 已使用 `cloudflared.exe` 启动临时 HTTPS 隧道。
- 当前 HTTPS 验收入口：
  `https://muslim-wheel-comparison-passenger.trycloudflare.com`
- 当前 HTTPS 云端礼物验收链接：
  `https://muslim-wheel-comparison-passenger.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`

## Stage 8 HTTPS 隧道验收入口今日验证

- `https://muslim-wheel-comparison-passenger.trycloudflare.com/` 返回 200。
- `https://muslim-wheel-comparison-passenger.trycloudflare.com/gift/gift-mqv7kqc8-10a79722` 返回 200。
- `cloudflared.log` 显示 Cloudflare connectivity pre-check 全部 PASS，使用 QUIC 连接。

## Stage 8 HTTPS 隧道验收入口待办事项

- 使用手机打开 HTTPS 云端礼物链接，验证标题、祝福、主题、背景图、音量入口和上传音频播放。
- 在 iPhone Safari、Android Chrome、微信内置浏览器中分别打开同一链接。
- 在 HTTPS 链接下测试“点击启动摄像头”，确认权限提示、失败降级和触摸兜底行为。

## Stage 8 HTTPS 隧道验收入口风险 / 阻塞

- trycloudflare 临时隧道没有稳定性保证，地址可能失效；如果失效，需要重新启动 cloudflared 获取新地址。
- 当前是本地 dev server + 临时隧道，不等同于正式 Vercel 部署；最终发布仍需配置正式 HTTPS 环境变量。

## Stage 8 HTTPS 隧道验收入口下一步计划

- 用户使用手机/微信/Safari/Chrome 打开 HTTPS 礼物链接并反馈结果；若通过，继续记录 Stage 8 跨设备验收通过并准备 Vercel 部署。

---

## Stage 8 HTTPS production tunnel fix

- User reported that the original trycloudflare dev-server link stalled on mobile: uploads and preset selections did not reflect success, preview stayed at `正在整理礼物预览`, and `/gift/gift-mqv7kqc8-10a79722` stayed at `正在打开这份礼物`.
- Root cause found in `cloudflared.log`: the tunnel was pointing at `next dev`, and repeated `/_next/webpack-hmr` websocket requests failed with `malformed HTTP response "Unauthorized"`.
- Stopped the old dev tunnel and started a production-mode Next server on port `3001`.
- Started a new Cloudflare tunnel for production validation: `https://rugby-yen-incidents-verified.trycloudflare.com`.
- Verified the new tunnel:
  - `/` returns 200.
  - `/gift/gift-mqv7kqc8-10a79722` returns 200.
  - `/api/gifts/gift-mqv7kqc8-10a79722` returns 200 and includes uploaded Supabase audio/image data.
  - The gift page HTML no longer contains a `webpack-hmr` marker.
- Added missing default theme background assets under `public/theme-backgrounds/`: `sakura.jpg`, `morning.jpg`, `cream.jpg`, `blue.jpg`.
- Verified all four theme background URLs return `200 image/jpeg` through the new HTTPS tunnel.

## Stage 8 next validation instruction

- Ignore the old `https://muslim-wheel-comparison-passenger.trycloudflare.com` link because it was backed by `next dev`.
- Use `https://rugby-yen-incidents-verified.trycloudflare.com` for the next mobile/browser pass.
- If this pass succeeds, record Stage 8 temporary HTTPS/mobile validation as passed and continue to Vercel deployment preparation.
- If this pass fails, capture the device/browser, exact page, and stuck text/button state, then inspect production tunnel logs and client flow.

---

## Stage 8 mobile gift UX and music controls

- User validated the production tunnel basic flow, then reported two Stage 8 mobile issues:
  - The gesture guide / interaction guide did not adapt well to small screens, making the bottom flow button unreachable.
  - Paused music restarted from the beginning; user wants pause to be temporary and resume from the previous point.
  - User also requested a delete-music action so shared gifts can have no BGM.
- Implemented guide scrolling fix:
  - `GiftExperience` adds a `guide-open` class while the guide is visible.
  - `globals.css` allows vertical panning/scrolling on the guide overlay/card while preserving the normal no-scroll touch interaction after the guide closes.
- Implemented music pause/resume fix:
  - Uploaded audio is paused without resetting `currentTime`.
  - Reopening music resumes the same `HTMLAudioElement`.
  - System BGM uses `AudioContext.suspend()` / `resume()` for temporary pause.
- Implemented delete music:
  - `/create/song` current selection block now has `删除音乐`.
  - The action clears `audioUrl` and `bgmPresetId`, sets `musicSelected` to false, and saves the draft.
  - `GiftExperience` only renders `SynthBgmButton` when `gift.musicSelected` is true, so shared no-music gifts do not play BGM.
- Build validation passed with `npm.cmd run build`.
- Gesture debug/tuning panel was not changed yet. Proposed product direction: remove the raw debug panel from recipient-facing pages, or replace it with simple user-facing controls only after confirmation.

---

## Stage 8 confirmed interaction flow refinement

- Confirmed product direction with the user before coding:
  - Guide primary button becomes `查看礼物生成`.
  - `完成并分享` appears only after the guide closes and the user reaches the real interaction page.
  - Guide can scroll from the center area; the real interaction page remains non-scrolling and uses small-screen responsive compression.
  - Music panel should clearly support play/pause/resume, volume, close BGM, and restore BGM.
  - Share-link generation gets progress feedback now; pre-upload remains a later module.
- Implemented `GiftExperience` guide state callback and configurable guide completion label.
- Hidden creator share/footer controls and recipient share action while the guide overlay is open.
- Updated preview footer to appear after the guide is dismissed.
- Updated `SynthBgmButton`:
  - Status button now reflects `无 BGM` / `播放音乐` / `音乐播放中`.
  - Popover includes horizontal volume control, `暂停音乐` / `继续播放`, `关闭 BGM` / `恢复 BGM`, and `收起`.
  - `关闭 BGM` is a temporary page-level audio state; the dedicated `/create/song` delete-music action remains the share-affecting removal path.
- Added small-height full-screen gift layout adjustments so the garden remains the primary visual while controls consume less vertical space.
- Added `saveCloudGift` progress callback and preview share messages for upload-audio, upload-image, and save-gift stages.
- Build validation passed with `npm.cmd run build`.

---

## Stage 8 bottom controls and system share feedback

- User reported that the real interaction page bottom controls (`音乐播放中` and barrage/share button) covered the gift card content.
- Moved the flower garden and music box upward by increasing `.music-box-wrap` bottom padding from `34px` to `88px`.
- Adjusted small-height responsive rules so the main visual stays dominant while leaving bottom control clearance:
  - `max-height: 720px`: bottom padding `72px`, garden `scale(0.9) translateY(-8px)`.
  - `max-height: 640px`: bottom padding `62px`, garden `scale(0.84) translateY(-10px)`.
- Confirmed system share implementation status:
  - It was implemented through `navigator.share`.
  - In `/create/preview`, unsupported browsers silently fell back to copying the link, making `系统分享` feel identical to `复制链接`.
- Updated `/create/preview` system share behavior:
  - If `navigator.share` is available, call native share and show a success/cancel message.
  - If unavailable, show an explicit unsupported message instead of silently copying.
- Updated `/gift/[id]` native share failure feedback before showing the copy-link popover.
- Build validation passed with `npm.cmd run build`.

## Stage 8 system share copy fallback wording

- Updated the creator preview share popover system-share button copy from `系统分享` to `分享至`.
- Kept native Web Share behavior when `navigator.share` is available.
- When native share is unavailable, canceled, or refused, the app now copies the link by default and shows: `当前系统暂不支持该功能，链接已复制，可直接粘贴分享。`
- Applied the same copy fallback to recipient gift-page share failure/unsupported paths.
- Build validation passed with `npm.cmd run build`.

## Stage 8 guide CTA fixed action area trial

- User approved trying the low-risk guide-only layout fix.
- Updated the guide overlay card into two areas:
  - `.guide-scroll`: the teaching/status content scrolls inside the card.
  - `.guide-actions`: the bottom action buttons remain visible inside the guide card.
- This change only affects the guide overlay. The real flower interaction page remains non-scrolling and keeps the flower/card as the main visual.
- Added card max-height constraints using `dvh/svh` so small mobile screens can always reach `查看礼物生成`.
- Build validation passed with `npm.cmd run build`.

---

## Stage 8 recipient forwarding title edit

- Clarified validation entry points:
  - The base HTTPS URL is the creation flow entry.
  - `/gift/[id]` is only the recipient gift display page.
- Updated the recipient gift page share flow:
  - The floating `转发` button now opens the share panel first instead of immediately invoking native share.
  - The share panel includes `礼物卡名字` editing.
  - `更新转发链接` saves the same gift as a new cloud gift with the edited title.
  - The new `/gift/[id]` URL is shown in the panel and can be copied or shared via Web Share.
  - `分享至` still uses native `navigator.share` when supported, with copy fallback and explicit unsupported/cancel messaging.
- Rebuilt the production app with `npm.cmd run build`.
- Restarted the production server on port `3001`.
- Verified current HTTPS tunnel endpoints return 200:
  - `https://andrews-keywords-bachelor-vegas.trycloudflare.com/`
  - `https://andrews-keywords-bachelor-vegas.trycloudflare.com/create/preview`
  - `https://andrews-keywords-bachelor-vegas.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`

## Stage 8 recipient forwarding title edit validation plan

- Creation-flow validation should use `https://andrews-keywords-bachelor-vegas.trycloudflare.com/`, not a `/gift/...` link.
- Gift-page validation should use `https://andrews-keywords-bachelor-vegas.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`.
- On the gift page, tap `转发`, edit `礼物卡名字`, tap `更新转发链接`, then copy/open the newly generated link and confirm the displayed gift name changed.

---

## Stage 8 share generation pre-upload reliability fix

- User reported that generating links could take nearly two minutes, sometimes failed, and fallback links lost uploaded BGM.
- Implemented pre-upload for uploaded audio on `/create/song`:
  - Reads the local file for immediate preview.
  - Normalizes empty mobile MIME data URLs based on file extension.
  - Uploads the audio to cloud storage immediately after selection.
  - Saves the cloud URL back into the draft so share generation no longer uploads the large audio file.
- Implemented pre-upload for uploaded background images on `/create/content`:
  - Compresses locally for preview.
  - Uploads the compressed image to cloud storage immediately.
  - Saves the cloud URL back into the draft.
- Updated draft merge behavior on `/create/content` and `/create/preview` so cloud resource URLs from IndexedDB are preferred over stale `data:` URLs from localStorage.
- Updated share generation failure behavior:
  - If uploaded audio/image is still only local and cloud upload fails, the app no longer returns a self-contained fallback link that would lose uploaded resources across devices.
  - The share panel now keeps the link empty and asks the user to wait for cloud upload success / retry.
- Extended upload MIME support for mobile `.m4a` variants: `audio/m4a` and `audio/x-m4a`.
- Repaired encoding-damaged JSX strings in the touched creation pages and verified the app compiles.
- Validation performed:
  - `npm.cmd run build` passed.
  - Local production server restarted on port `3001` with PID `11496`.
  - New Cloudflare tunnel: `https://blocked-tuning-chapter-operates.trycloudflare.com`.
  - `/`, `/create/preview`, and `/gift/gift-mqv7kqc8-10a79722` all returned 200.
  - `/api/upload` audio smoke test returned 200 and a Supabase Storage URL.

## Stage 8 share generation pre-upload validation plan

- Use `https://blocked-tuning-chapter-operates.trycloudflare.com/` for the creation flow.
- Upload audio in method 3 and wait until the message says the audio has uploaded to cloud storage.
- Upload or change the background image and wait until the message says the image has uploaded to cloud storage.
- Continue to preview, open the share panel, edit the gift name, and generate the link.
- The link should be generated much faster than before because large resources were uploaded earlier.
- Open the generated `/gift/...` link on another browser/device and confirm uploaded BGM is still used.
- On the gift page, tap `转发`, edit `礼物卡名字`, tap `更新转发链接`, then open the new link and confirm the name changed.

---

## Stage 8 creation-flow text encoding repair

- User reported that the recipient gift page was fine, but the creation entry and newly generated gift text were mojibake after the previous fix.
- Repaired user-visible Chinese copy in:
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - default theme/title/blessing data in `src/lib/gift.ts`
- Fixed encoding-damaged comments that were swallowing code in `src/lib/gift.ts` and `/create/song`.
- Rebuilt with `npm.cmd run build` successfully.
- Restarted production server on port `3001`; current listener PID is `15152`.
- Verified local visible page text in browser:
  - `/` shows normal Chinese homepage text.
  - `/create/song` shows normal Chinese selection/upload/BGM text.
  - `/create/content` shows normal Chinese content/theme/edit controls and normal theme names.
  - `/create/preview` shows normal Chinese default title and blessing text.
- Verified current tunnel endpoints return 200:
  - `https://blocked-tuning-chapter-operates.trycloudflare.com/`
  - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
  - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/content`
  - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`

## Stage 8 creation-flow text encoding validation plan

- Hard refresh `https://blocked-tuning-chapter-operates.trycloudflare.com/` or open it in a private browser tab.
- Confirm the home and creation pages show normal Chinese text.
- Create a new gift from scratch and generate a new link.
- Open the newly generated gift link and confirm the gift title, theme name, BGM info, and blessing are not mojibake.

---

## Stage 8 share generation speed and product wording fix

- User clarified that product copy should not expose engineering wording such as resource cloud upload.
- Updated creation flow messages to use user-facing states:
  - Audio: `正在准备音乐...` / `音乐已准备好，生成链接时会更快，并会保留这段音乐。`
  - Image: `正在准备图片...` / `图片已准备好，生成链接时会更快。`
  - Share: `正在生成礼物链接...`
- Added a fast guard before share generation:
  - If uploaded music or image is still only local, the share panel now immediately shows `礼物还没有准备好，请稍等一下再生成链接。`
  - This avoids making the user wait for a long, uncertain link-generation attempt.
- Disabled moving from upload steps while media is still preparing:
  - `/create/song` blocks the next step while audio is preparing.
  - `/create/content` blocks preview while image is preparing.
- Cloud gift upload progress is now only shown when there is a real local `data:` asset to prepare, so saved cloud URLs skip unnecessary upload progress.
- Replaced the remaining upload failure fallback copy with product-facing `礼物还没有准备好。`
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `33264`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/content`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`
  - Local API smoke save completed in about 1.7 seconds after media was already represented by URLs.

## Stage 8 share generation speed validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/` in a fresh or private mobile browser tab.
- Choose method 3 to upload music and wait until it says the music is ready.
- Upload or select the background image and wait until it says the image is ready.
- Continue to preview, open the share panel, edit the gift name if needed, then tap generate.
- Expected result: once music/image show ready, generating the link should take seconds rather than minutes.
- Early-click validation: if you try to generate before media is ready, it should quickly show `礼物还没有准备好，请稍等一下再生成链接。` without spinning for minutes.
- Open the generated gift link on another browser/device and confirm the uploaded BGM is retained.

## Stage 8 next step

- Continue with mobile end-to-end regression for Stage 8:
  - creation flow text,
  - music and image preparation,
  - share generation speed,
  - uploaded BGM retention,
  - gift-page name editing and re-share behavior.

---

## Stage 8 preparation timing UX adjustment

- User asked whether preparation waits should be spread across every creation step.
- Adjusted the interaction model so waiting is not evenly distributed:
  - `/create/song` no longer blocks `下一步` while uploaded music is preparing.
  - `/create/content` no longer blocks `预览礼物` while uploaded image is preparing.
  - Upload steps keep lightweight status messages and continue preparing in the background.
  - `/create/preview` share panel now centralizes readiness:
    - `礼物内容已完成`
    - `音乐准备中` / `音乐已准备好`
    - `图片准备中` / `图片已准备好`
  - Share generation remains the final gate. If music or image is still local-only, it immediately tells the user that media is still preparing and does not enter a long generation attempt.
- Also removed a duplicate draft persistence call in `/create/song`.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `31984`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/content`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`

## Stage 8 preparation timing validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/` in a fresh mobile browser tab.
- Upload music, then immediately tap `下一步`; it should allow moving forward instead of forcing a wait.
- Upload a background image, then immediately tap `预览礼物`; it should allow entering preview instead of forcing a wait.
- Open the share panel on preview:
  - Confirm it shows gift/music/image readiness states.
  - If music or image is still preparing, the generate button should read `准备中，稍后生成`.
  - Tapping it should quickly explain that music or image is still preparing, without spinning for minutes.
- Wait until the panel shows music and image ready, then generate the link.
- Confirm generation is seconds-level and the generated gift link retains uploaded BGM/background.

## Stage 8 next step after validation

- If this timing feels good, continue with Stage 8 mobile end-to-end regression.
- If it still feels slow, the next focused module should be visible background upload recovery / retry from the preview share panel.

---

## Stage 8 media preparation speed trial

- User confirmed the current preparation interaction is acceptable and asked to improve overall music/image preparation speed without changing the flow.
- Implemented a low-risk speed trial:
  - Background image compression now uses `640px` max width and `0.66` JPEG quality instead of `800px` / `0.75`.
  - This keeps enough fidelity for a decorative gift background while reducing upload payload size.
- Added preview-page recovery preparation:
  - If `/create/preview` receives uploaded music or image that is still a local `data:` URL, it now uploads it in the background while the user reviews the gift or edits the share title.
  - When upload finishes, it writes the cloud URL back to localStorage, IndexedDB, and in-memory draft state.
  - The write-back checks that the source `data:` URL is still the current one, so a later user edit is not overwritten by an older upload finishing late.
- Kept the current interaction unchanged:
  - Song/content pages still allow moving forward immediately.
  - The share panel still remains the final generation gate.
  - No new buttons or steps were added.
- Validation performed:
  - First build caught and fixed a TypeScript null-narrowing issue in the preview recovery task list.
  - `npm.cmd run build` passed after the fix.
  - Production server restarted on port `3001`; current listener PID is `33240`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/content`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`

## Stage 8 media preparation speed validation plan

- Use the same image and music files as the previous test so timing is comparable.
- Upload music, immediately continue forward, then upload image and immediately enter preview.
- Stay on preview for a short moment, open `完成并分享`, and watch whether readiness changes from `准备中` to `已准备好` without leaving the page.
- Generate the link only after both are ready.
- Expected result:
  - Image preparation should complete faster than before.
  - Preview waiting time should feel shorter because it now continues preparing in the background.
  - Link generation should remain seconds-level once both media items are ready.
  - The generated gift link should retain uploaded BGM and background image.

## Stage 8 next step after speed validation

- If speed is acceptable, continue with full Stage 8 mobile regression.
- If music remains the slow part, the next focused module should be an audio-specific strategy: stricter upload size guidance, duration trimming, or browser-supported audio transcoding investigation.

---

## Stage 8 active preview media preparation

- User proposed that music and image should prepare while the creator is experiencing the preview interaction page, instead of waiting until `完成并分享`.
- Upgraded the speed trial into an active preview preparation mechanism:
  - Added `src/lib/mediaPreparation.ts` as a shared per-tab preparation task registry.
  - Song upload, image upload, and preview preparation now reuse the same in-flight task for the same local `data:` resource.
  - This prevents duplicate uploads when the user quickly moves from upload pages into preview.
  - `/create/preview` now actively starts or resumes music/image preparation immediately when the preview interaction page receives local uploaded media.
  - Music and image are written back independently: whichever finishes first updates the draft first.
  - Write-back still checks that the source local media is unchanged, preventing an older upload from overwriting a newer edit.
- Kept the accepted interaction unchanged:
  - Upload pages still allow immediate forward movement.
  - Preview interaction remains the natural waiting/experience phase.
  - Share panel remains the final readiness gate.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `1992`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/content`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`

## Stage 8 active preview preparation validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/` in a fresh mobile browser tab.
- Upload music and immediately continue forward.
- Upload image and immediately enter preview.
- Stay on the preview interaction page for 10-30 seconds and interact with the flower.
- Then tap `完成并分享`.
- Expected result:
  - The share panel should more often already show `音乐已准备好` and `图片已准备好`.
  - If one item is still preparing, only that item should remain in `准备中`.
  - Tapping generate while still preparing should fail quickly with the current readiness prompt, not wait for minutes.
  - Once both are ready, link generation should be seconds-level.
  - The generated gift link should retain uploaded BGM and background.

## Stage 8 next step after active preview validation

- If preview-time preparation solves the perceived wait, continue with full Stage 8 mobile regression.
- If uploaded music still takes too long even after preview-time preparation, the next module should be audio-specific compression/duration strategy.

---

## Stage 8 guide compression, audio limit, and gesture prewarm

- User reported three validation issues:
  - Gesture guide still required too much scrolling to reach the bottom buttons.
  - Image preparation was acceptable, but music preparation was still slow.
  - Camera / gesture recognition startup was still too slow, and touch-first fallback would weaken the core hand-recognition highlight.
- Implemented the accepted first two fixes and the revised third strategy:
  - Compressed the gesture guide UI into a shorter hand-recognition-first card.
  - Replaced long default guide copy with three concise steps: allow camera, raise palm, wave to interact.
  - Moved camera diagnostics into a collapsed details section so it no longer consumes the first screen.
  - Kept hand gesture as the primary CTA: `开启手势识别` / `正在打开摄像头` / `进入手势互动`.
  - Added early MediaPipe hand model prewarm while the guide is visible, before the user taps the camera CTA.
  - Shared the hand-model loading promise so prewarm and camera startup do not duplicate model loading.
  - Tightened uploaded audio strategy:
    - Recommended size: 5MB.
    - Hard limit: 6MB.
    - Files above 5MB get a clear “may take longer” message.
    - Files above 6MB ask the user to compress or trim to about 60 seconds.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `14960`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/gift/gift-mqv7kqc8-10a79722`
  - Browser mobile viewport check at `390 x 667`:
    - Guide card height about 545px.
    - Primary hand-gesture button visible in the first viewport.
    - Bottom secondary button visible in the first viewport.

## Stage 8 guide/audio/camera validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview` on a small phone screen.
- Confirm the hand gesture guide is mostly one screen:
  - Title, camera status, three short steps, and bottom buttons should be visible without difficult scrolling.
  - Diagnostics should be collapsed under `摄像头状态`.
- Tap `开启手势识别`:
  - The button should move through a clearer startup state.
  - The model should already be warming while the guide is open, so recognition should feel faster than before.
  - If the browser shows a camera permission prompt, allow it and confirm the guide moves into live hand-recognition status.
- In the music upload flow:
  - A file over 6MB should be rejected with a clear trim/compress message.
  - A file over 5MB but under 6MB should be allowed but warned as slower.
  - A smaller file should prepare normally.

## Stage 8 next step after this validation

- If guide visibility and camera startup feel acceptable, continue full Stage 8 mobile regression.
- If camera startup still feels slow after model prewarm, the next focused module should be camera stream startup optimization and model asset caching strategy.

---

## Stage 8 guide and large-audio validation fixes

- User reported follow-up validation issues:
  - Gesture guide spent too much space explaining camera startup.
  - Specific gesture instructions and touch fallback were not clear enough.
  - After camera opens, the guide still could not fully show bottom buttons.
  - The >5MB audio warning was not prominent.
  - >6MB audio should explain compression may reduce quality and let the user choose continuing compression or replacing audio.
- Updated the compact gesture guide:
  - Rebalanced copy from camera setup toward gestures.
  - Steps now cover: allow camera, move hand up/down, wave left/right, and touch fallback.
  - Secondary guide button now reads `触屏查看礼物生成` before camera mode, making the touch option explicit.
  - Reduced compact camera preview height so camera-on guide state leaves more room for bottom buttons.
  - Shortened compact camera message max height so status text does not push actions down.
- Updated large audio handling:
  - 5-6MB still shows a warning and continues.
  - 6-10MB now pauses and shows a prominent choice panel.
  - The panel says continuing compression may reduce sound quality and offers `继续压缩` or `更换音频`.
  - >10MB remains blocked with a clear shorter-audio message.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `29948`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`

## Stage 8 guide and large-audio validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview` on mobile.
- Confirm the guide is no longer dominated by camera-opening instructions.
- Confirm it clearly explains:
  - up/down hand movement,
  - left/right hand movement,
  - touch fallback via `触屏查看礼物生成`.
- Tap `开启手势识别`, allow camera, and confirm the bottom buttons are still reachable after the camera preview appears.
- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`.
- Upload audio:
  - 5-6MB: warning should be visible but upload continues.
  - 6-10MB: prominent panel should appear with `继续压缩` and `更换音频`.
  - >10MB: should ask for a shorter audio file.

## Stage 8 next step after this validation

- If these fixes pass, continue full Stage 8 mobile regression.
- If camera-on guide still cannot show bottom buttons on the target phone, the next module should remove the live preview from the guide card and keep only a small status strip until entering the full interaction view.

---

## Stage 8 gesture and touch tutorial separation

- User reported that the compact guide still missed two hand gestures and mixed touch mode with the gift-generation action.
- Updated the guide structure:
  - Camera permission / readiness is now a small status row.
  - Gesture tutorial is the main content and includes four compact actions:
    - up/down hand movement,
    - left/right hand movement,
    - fist,
    - index-finger + thumb pinch.
  - Added an independent `guideTutorialMode` state so tutorial switching does not mutate the actual camera/touch interaction mode.
  - Added a dedicated `切换触屏教程` button.
  - Touch tutorial now shows touch-specific actions:
    - up/down swipe,
    - left/right swipe,
    - tap flower,
    - double tap screen.
  - `查看礼物生成` is now a separate button from tutorial switching.
- While fixing the guide, repaired several encoding-damaged strings in `GiftExperience.tsx` that had become build-breaking:
  - gesture labels,
  - camera status labels,
  - camera error messages,
  - data panel labels,
  - camera launch button labels,
  - music note glyphs.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `24236`.
  - Current tunnel endpoints returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/song`
  - Browser mobile viewport check at `390 x 667`:
    - Guide action area top is about `500px`, bottom about `612px`, inside first viewport.
    - Gesture tutorial includes `握拳` and `食指拇指捏合`.
    - `切换触屏教程` changes the guide content to touch-specific controls.

## Stage 8 gesture/touch tutorial validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview` on mobile.
- Confirm the default guide teaches hand gestures, not only camera setup:
  - `上下抬手`
  - `左右挥手`
  - `握拳`
  - `食指拇指捏合`
- Confirm camera permission/readiness is visually separate from the gesture tutorial.
- Tap `切换触屏教程`.
- Confirm the tutorial changes to touch controls:
  - `上下滑动`
  - `左右滑动`
  - `点击花朵`
  - `双击屏幕`
- Confirm `查看礼物生成` remains a separate button and does not serve as the touch-mode switch.

## Stage 8 next step after this validation

- If guide/touch tutorial separation passes, continue full Stage 8 mobile regression.
- If camera-on state still pushes actions out of view on the target phone, remove the live camera preview from the guide card and keep it only in the full interaction scene.

---

## Stage 8 remove user-facing gesture tuning panel

- User noticed a `调参` panel on the preview page and asked whether it would be shown to real users.
- Confirmed that `GestureDebugPanel` was being rendered in `GiftExperience` whenever camera mode was active.
- Removed the debug/tuning panel from the user-facing gift experience:
  - `GestureDebugPanel` component remains in the codebase for future internal use.
  - `GiftExperience` no longer imports or renders the panel.
  - Gesture config loading remains available for the interaction logic.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `11244`.
  - Current tunnel preview route returned 200:
    - `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`
  - Searched the render file and confirmed `GestureDebugPanel` is no longer rendered from `GiftExperience`.

## Stage 8 tuning panel validation plan

- Open `https://blocked-tuning-chapter-operates.trycloudflare.com/create/preview`.
- Start hand gesture mode.
- Confirm no `调参` button or parameter panel appears over the flower/card UI.
- Confirm camera gesture, music controls, and share/gift-generation buttons still work.

## Stage 8 next step after this validation

- Continue full Stage 8 mobile regression if the tuning panel is gone.

---

## Stage 8 preview music control regression fix

- User reported that the preview page music component disappeared.
- Root cause:
  - `GiftExperience` rendered `SynthBgmButton` only when `gift.musicSelected` was true.
  - The default gift has `musicSelected: false` but still has a playable default BGM (`晨光花园`), so the music control was hidden on default preview drafts.
- Fixed the render condition:
  - Added `hasPlayableMusic(gift)`.
  - Shows music controls when:
    - music has been selected,
    - uploaded audio exists,
    - a system BGM preset exists,
    - or the draft is still using the default BGM.
  - Still hides the control when the user explicitly deletes music and the title is `未选择背景音乐`.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `6500`.
  - Local preview route returned 200:
    - `http://127.0.0.1:3001/create/preview`
  - Browser check on local preview confirmed:
    - `.music-controls` renders content,
    - music button text is `播放音乐`,
    - gift text still contains `晨光花园`.
- Current risk:
  - The previous Cloudflare tunnel returned 530 during validation, while local preview remained healthy.

## Stage 8 preview music control validation plan

- On the local in-app browser, open `http://127.0.0.1:3001/create/preview`.
- Confirm the lower-left music control appears again.
- Confirm the button says `播放音乐` before playing.
- Confirm explicit `删除音乐` still hides BGM in generated gifts.

---

## Stage 8 fast local gift-link save

- User reported that link generation was still slow even after the music-size strategy.
- Diagnosis:
  - Once music/image are prepared, link generation should only save gift JSON.
  - The previous `/api/gifts` implementation waited for Supabase `upsert`, so link generation could still feel slow because of remote database latency.
  - The `继续压缩` copy was also misleading because the current app does not perform real client-side audio transcoding.
- Implemented fast local save for the current local/tunnel validation environment:
  - Added `src/lib/serverGiftStore.ts`.
  - `/api/gifts` now writes the gift JSON to `.data/cloud-gifts.json` first and returns immediately.
  - Supabase save is still attempted in the background when configured, but it no longer blocks link generation.
  - `/api/gifts/[id]` now reads local saved gifts first, then falls back to Supabase.
  - Added `.data/` to `.gitignore`.
- Corrected large-audio copy:
  - Replaced `继续压缩` with `继续使用原音频`.
  - Copy now explains that large audio will take longer and the app does not alter the original audio.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `32940`.
  - Local save timing:
    - `POST /api/gifts` returned 200 in about `355ms`.
    - `GET /api/gifts/gift-mqwa5gkd-ebd06542` returned 200 in about `755ms`.

## Stage 8 fast local gift-link save validation plan

- Open `http://127.0.0.1:3001/create/preview`.
- Ensure the share panel shows music/image ready before generating.
- Tap generate link.
- Expected result:
  - Link generation should be seconds-level, normally close to instant on local service.
  - The generated `/gift/...` link should open successfully while this local server/tunnel is running.
- Important boundary:
  - If uploaded music still shows `准备中`, generation should not proceed yet; this change accelerates gift JSON saving, not the actual audio upload/preparation step.

---

## Stage 8 guide fallback state correction

- User said the current preview interface was still not right.
- Inspected the in-app browser state and found a bad fallback combination:
  - Camera permission had previously been denied.
  - The guide still showed hand-gesture tutorial content while the camera state was `触摸备用`.
  - This made the UI say `开启手势识别` while also explaining a fallback state, which was confusing.
- Updated guide state handling:
  - When `cameraStage` becomes `touch-fallback`, the guide automatically switches to the touch tutorial.
  - In touch tutorial, the main action is `使用触屏互动`.
  - The secondary action becomes `重新开启手势`, which retries camera access instead of only changing tutorial text.
- Validation performed:
  - `npm.cmd run build` passed.
  - Production server restarted on port `3001`; current listener PID is `9276`.
  - Local preview route returned 200.
  - Browser check confirmed:
    - music control still displays `播放音乐`,
    - guide loads in normal gesture tutorial state after page refresh.

## Stage 8 context and debugging documentation

- User requested a durable handoff context for the next session and explicit development rules for repeated problems.
- Added `docs/16-stage-8-context-and-debugging.md` as the clean Stage 8 source of truth.
- The new document records:
  - current routes and validation entry points,
  - key product and architecture decisions,
  - completed Stage 8 modules,
  - important modified files,
  - current data flow,
  - common failure causes and development rules,
  - next-session acceptance checklist,
  - known risks and next recommended module.
- Added a latest-context pointer to `docs/15-stage-8-handoff.md`.
- Added log ignore rules for local Cloudflare and production server scratch logs.
- Note: older handoff/rules docs still contain historical mojibake, so `docs/16-stage-8-context-and-debugging.md` should be treated as the clean continuation document for Stage 8.

## Stage 8 context validation plan

- In the next session, read `docs/16-stage-8-context-and-debugging.md` before making changes.
- Verify the next task against the documented route map:
  - `/create/song` for music upload/selection,
  - `/create/content` for image/theme/content,
  - `/create/preview` for interactive preview and share generation,
  - `/gift/[id]` for recipient gift validation.
- When a repeated issue appears, use the corresponding playbook section before editing code.

---

## Stage 8 guide fallback validation plan

- If camera permission is denied, reopen the preview page.
- Expected result:
  - The guide should show touch tutorial content rather than hand-gesture instructions.
  - Main button should say `使用触屏互动`.
  - Secondary button should say `重新开启手势`.
- If camera permission is not denied, default guide should show the gesture tutorial.
