# BloomBeat 开发日志 - 2026-06-24

## 今日完成

- 根据用户阶段 1 体验反馈调整交互页。
- 将触摸交互从松手后响应改为拖动中连续响应，降低触发阈值，提升灵敏度。
- 在礼物页顶部增加 `给XX的礼物` / 用户称呼标题，并显示当前歌曲名。
- 将音乐盒主体从单朵花改为 5 朵花组成的小花园。
- 将花朵换色扩展为红、黄、蓝、紫、粉五色循环，不再局限于主题色。
- 音乐进入页面时尝试自动播放；按钮在播放时用于暂停，若浏览器拦截自动播放则显示点击播放。
- 根据用户第二轮反馈继续调整阶段 1 交互页。
- 将花园数量增加为 8 朵，并调整为视觉居中、拉开间距，减少张开时重叠。
- 增加草地和飘动音符，让花从音乐草地上长出来。
- 调整花朵换色逻辑：初始色与主题一致，后续换色时整片花园统一随机变成新颜色。
- 修复暂停音乐后 BGM 仍可能继续响的问题。
- 在礼物内容页增加祝福语占比选择：15%、30%、50%、75%。
- 增加上下 / 左右方向辅助按钮。
- 将 `给XX的礼物` 从顶部移入 BloomBeat 音乐盒便签内。
- 根据用户第三轮反馈，删除上下 / 左右方向辅助按钮。
- 调整左右挥动效果：只让花朵随风摇动，草地和 BloomBeat 便签保持稳定。
- 增加风吹 / 水波纹式波动层，强化左右挥动时的风感。
- 将音乐控制按钮改成喇叭图标；点击喇叭进入静音并关闭 BGM。
- 明确记录：花朵样式和全站 UI 风格精修放到阶段 5，当前阶段只修核心体验问题。
- 修复点击喇叭按钮会被父级触摸区域误判为花朵触碰的问题。
- 增加高位叶子，并让叶子随植物高度变化一起上升。
- 增强无操作时花朵自然摇晃幅度，且不同花朵摇晃节奏不同。
- 左右滑动后根据风向和风力改变花朵倾斜与摇晃。
- 风力面板从纯数值改为 `东风 +X`、`西风 +X` 或 `微风 0`。
- 增大花朵升高和降低的视觉幅度。
- 优化单击 / 双击判定：单击延迟确认开合，双击会取消第一次单击开合并只触发统一换色。
- 将无操作摇晃调整为风铃绳子式的缓慢、轻柔摆动。
- 将祝福语占比改为从页面顶部开始计算。
- 在礼物内容页增加祝福语行距调节。
- 将祝福语字号范围调整为 5px - 24px。
- 将祝福语行距调整为基于祝福语占比区域的 1x - 3x。
- 交互页祝福语改为波浪线样式流动显示。
- 将喇叭图标移到左下角，并将花园、草地和 BloomBeat 便签整体下移。
- 再次增大花朵上下移动最大幅度。
- 调整东西风逻辑：微风 0 保持轻柔摆动；东风 / 西风数值越大，花朵摇晃弧度越大，且每朵花幅度不同。
- 同步更新 `docs/01-prd.md` 与 `docs/08-acceptance-checklist.md`。

## 今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已启动本地 dev server 并验证以下路由均返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 第二轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第二轮反馈调整后，已再次验证上述路由均返回 200。
- 第三轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第三轮反馈调整后，已再次验证上述路由均返回 200。
- 第四轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第四轮反馈调整后，已再次验证上述路由均返回 200。
- 第五轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第五轮反馈调整后，已再次验证上述路由均返回 200。
- 第六轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第六轮反馈调整后，已再次验证上述路由均返回 200。
- 将祝福语行距范围修正为 0.8x - 1.5x。
- 修正祝福语波浪效果：去掉波浪下划线，改为文字沿波浪轨迹流动。
- 进一步增大花朵摇晃幅度和向上生长最高值。
- 第七轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第七轮反馈调整后，已再次验证上述路由均返回 200。
- 将祝福语字号范围调整为 5px - 20px。
- 将祝福语行距范围调整为 0.5x - 1.2x。
- 将祝福语波浪效果改为字符组成波浪线形状并横向流动。
- 将花朵最高上升幅度提升为上一版的约 1.4 倍。
- 第八轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第八轮反馈调整后，已再次验证上述路由均返回 200。
- 再次放大东西风下花朵左右摆动幅度。
- 保持每行祝福语由字符组成同样的波浪形状，使多行波浪更平行。
- 在保留字符波浪形状的基础上，给每行祝福语流动轨迹增加轻微上下跳动。
- 第九轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第九轮反馈调整后，已再次验证上述路由均返回 200。
- 增大微风状态下花朵自然摆动幅度，并让不同花朵保持不同摆动节奏和幅度。
- 在风力旁增加循环按钮，点击后回到微风状态。
- 祝福语字符增加轻微独立跳动，同时保留整句波浪线轮廓。
- 第十轮反馈调整后，已再次运行 `npm.cmd run build` 并通过。
- 第十轮反馈调整后，已再次验证上述路由均返回 200。

## 待办事项

- 用户刷新浏览器后按更新后的阶段 1 验收项进行体验确认。
- 用户确认阶段 1 没有问题后，再进入阶段 2。

## 风险 / 阻塞

- 现代移动浏览器可能会拦截无用户手势触发的自动播放。当前实现会尝试默认播放；如果被浏览器拦截，按钮会提示用户点击播放。
- 全站 UI 风格统一精修建议放在阶段 5；阶段 1 仅修正影响核心体验的交互页布局和视觉问题。

## 下一步计划

- 等待用户确认阶段 1 体验是否通过。

---

## 阶段 2 今日完成

- 在选歌页增加本地音频上传能力。
- 音频上传限制为 15MB 以内，支持 mp3 / wav / m4a。
- 在礼物内容页增加背景图片上传能力。
- 图片上传限制为 5MB 以内，不上传时继续使用主题模板背景。
- 交互页支持使用上传图片作为模糊背景。
- 交互页支持使用上传音频作为 BGM；未上传时继续使用默认合成 BGM。
- 预览页增加“生成礼物链接”和“复制链接”功能。
- 新增本地 IndexedDB 礼物存储兜底，支持生成 `/gift/[id]` 并在同一设备同一浏览器打开。
- `/gift/[id]` 支持读取保存后的礼物数据；`/gift/demo` 保留为当前草稿示例页。
- 新增 `.env.example`，记录后续 Supabase 配置字段。
- 更新技术文档和风险文档，说明未配置 Supabase 时为本地兜底模式。
- 修正上传音频后的播放逻辑：有用户上传音频时只播放上传音频，不叠加默认 / 主题 BGM。
- 调整音量映射：花越高音量越大，越矮音量越小；越靠东 / 西两端音量越大，越靠中间音量越小。
- 更新手势引导语，说明上下和左右滑动对音量的影响。
- 修正上传图片背景写法，使上传图片成为礼物页背景并叠加模糊蒙版。
- 将生成礼物链接从大面板改为右下角转发图标和小弹窗。
- 分享弹窗主按钮在未生成时显示“生成礼物链接”，生成后变为“复制链接”。
- 确认生成后的 `/gift/[id]` 链接只打开交互礼物页，不进入选歌、传图或编辑流程。

## 阶段 2 今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已启动本地 dev server 并验证以下路由均返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 阶段 2 修正后，已再次运行 `npm.cmd run build` 并通过。
- 阶段 2 修正后，已再次验证上述路由均返回 200。
- 调整左右滑动音量映射：越靠左音量越小，越靠右音量越大。
- 提高上下高度变化对音量的影响权重，让高低音量差距更明显。
- 更新手势引导语，说明左右滑动为左小右大。
- 将转发按钮改为粗线条弧形标志，样式与左下角喇叭按钮一致，并放在右下角对称位置。
- 本轮修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮修正后，已再次验证主要路由均返回 200。
- 将转发按钮移入礼物画面内部右下角，避免遮挡底部操作按钮。
- 将底部“查看礼物页”按钮改为“礼物效果”。
- 优化上传图片背景：降低蒙版强度和模糊强度，同时保留泡泡氛围，让用户图片更明显成为背景。
- 将“默认治愈系 BGM”文案改为“若无音频，可选系统 BGM”。
- 点击系统 BGM 板块时会选择该 BGM 并播放对应试听音色。
- 将图片上传说明改为“可上传 5MB 以内图片；不上传时默认为纯色主题背景”。
- 本轮体验修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮体验修正后，已再次验证主要路由均返回 200。
- 修复上传音频未在预览页使用的问题：上传音频和图片改为写入 IndexedDB 草稿，预览页和 demo 礼物页优先读取 IndexedDB 草稿。
- 调整 `saveDraft`，localStorage 只保存轻量字段，不再写入音频 / 图片 Data URL，避免容量限制导致资源丢失。
- 返回选歌页后重新选择系统 BGM 或上传音频时，会保留已有背景图片等草稿字段。
- 再次减轻上传图片背景的模糊和蒙版强度，让图片主体更清晰。
- 本轮上传资源修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮上传资源修正后，已再次验证主要路由均返回 200。
- 交互页上传图片背景模糊强度从 5px 降到 2.5px。
- 为上传图片增加 H5 展示区域选择控件，支持左右 / 上下位置调整。
- 新增 `backgroundPositionX` 和 `backgroundPositionY` 草稿字段，并在交互页背景中应用。
- 本轮图片展示修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮图片展示修正后，已再次验证主要路由均返回 200。
- 交互页上传图片背景模糊强度从 2.5px 降到 1px。
- 未上传图片前，内容页显示空白背景预览框。
- 为上传图片增加缩放滑杆，支持 70% - 180% 缩放。
- 新增 `backgroundScale` 草稿字段，并在交互页背景中应用。
- 本轮图片裁切修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮图片裁切修正后，已再次验证主要路由均返回 200。
- 交互页上传图片背景模糊强度从 1px 降到 0.3px。
- 图片缩放控件文案改为 `Zoom in`。
- 图片默认以高度填满 H5 画面，缩放范围改为 100% - 220%，只能放大不能缩小。
- 本轮图片 Zoom in 修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮图片 Zoom in 修正后，已再次验证主要路由均返回 200。
- 未上传图片前，背景预览框改为无底纹的空白“图片预览栏”。
- 用户上传图片后，交互页背景蒙版改为无色透明蒙版，不再跟随主题色。
- 交互页上传图片背景模糊强度从 0.3px 降到 0.09px。
- 本轮图片蒙版修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮图片蒙版修正后，已再次验证主要路由均返回 200。
- 将 `Zoom in` 改为额外放大量显示：0% 表示默认高度填满屏幕。
- 当 `Zoom in` 为 0% 时，上下位置滑杆禁用，并固定为 0%。
- 将交互页上传图片背景模糊强度设为 0.15px。
- 本轮 Zoom in 规则修正后，已再次运行 `npm.cmd run build` 并通过。
- 本轮 Zoom in 规则修正后，已再次验证主要路由均返回 200。
- 将交互页上传图片背景模糊强度从 0.15px 调整回 0.3px。
- 本轮模糊参数修正后，已再次运行 `npm.cmd run build` 并通过，主要路由均返回 200。
- 将交互页背景蒙版改为当前主题色 30% 透明度，背景模糊保持 0.3px。
- 确认未上传图片前，内容页背景预览为空白“图片预览栏”，不显示任何图片或主题图。
- 本轮蒙版规则修正后，已再次运行 `npm.cmd run build` 并通过，主要路由均返回 200。

## 阶段 2 待办事项

- 用户按阶段 2 验收清单验证：上传图片、上传 15MB 以内音频、超过 15MB 拒绝、生成 `/gift/[id]`、新窗口打开同一礼物。
- 若要跨设备真实分享，需要提供 Supabase 项目 URL、anon key、service role key，并创建 Storage bucket / Database 表。

## 阶段 2 风险 / 阻塞

- 当前没有 Supabase 凭证，因此阶段 2 先实现本地 IndexedDB 兜底。它可验证产品流程，但不能跨设备访问。
- Data URL 存储适合黑客松本机验证，真实上线应切换到 Supabase Storage URL。

## 阶段 2 下一步计划

- 等待用户确认本地阶段 2 验收结果。
- 用户确认后，可进入 Supabase 云端接入或继续阶段 3 音乐能力。

---

## 阶段 2 云端化今日完成

- 安装 `@supabase/supabase-js`，新增 Supabase 服务端 helper。
- 新增 `POST /api/upload`，支持将图片 / 音频 Data URL 上传到 Supabase Storage。
- 新增 `POST /api/gifts`，支持将 Gift 配置保存到 Supabase `gifts` 表。
- 新增 `GET /api/gifts/[id]`，支持按 id 读取云端 Gift。
- 预览页生成礼物链接时优先尝试 Supabase 云端保存。
- Supabase 未配置或云端保存失败时，自动回退到 IndexedDB 本地礼物链接。
- 收礼页 `/gift/[id]` 优先读取云端 Gift；云端不可用或未找到时，再读取本地 IndexedDB。
- 找不到礼物时补充云端 / 本地失败原因提示。
- 更新 `docs/06-api-and-data-spec.md`，补齐阶段 2 Supabase 表结构、Storage、API 行为和新增 Gift 字段。

## 阶段 2 云端化今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 未配置 Supabase 的场景下，代码路径会通过 API 503 触发前端本地 IndexedDB 兜底。
- 已按阶段 2 验收清单复核：当前改动只触碰分享生成、资源上传云端化和 `/gift/[id]` 读取优先级，不改变选歌、上传草稿、内容编辑、预览礼物和交互礼物主流程。

## 阶段 2 云端化待办事项

- 配置 `.env.local` 的 Supabase URL、service role key 和 bucket 后，进行真实跨设备链接验收。
- 在 Supabase 创建 `gifts` 表：`id text primary key`、`gift jsonb not null`、`created_at timestamptz default now()`。
- 确认 `bloombeat-gifts` bucket 可公开读取，或后续改为签名 URL 策略。

## 阶段 2 云端化风险 / 阻塞

- 当前环境未提供 Supabase 凭证，因此无法完成真实云端连通验收。
- Supabase Storage bucket 如果不是 public，当前返回的 public URL 将无法直接播放音频或展示背景图。
- `npm audit` 报告 2 个 moderate 漏洞；本次未执行强制修复，避免引入不相关依赖升级风险。

## 阶段 2 云端化下一步计划

- 填入 Supabase 配置后，验证上传图片、上传音频、生成云端链接、清空 IndexedDB 后仍能打开同一 `/gift/[id]`。
- 云端阶段 2 验收通过后，再进入阶段 3 音乐链接识别与 mock 推荐。

---

## 阶段 2 分享体验修正今日完成

- 优化预览页分享弹窗文案，改为面向用户的生成、复制和转发引导，不再展示 Supabase / IndexedDB 技术说明。
- 从预览页点击“礼物效果”时，为 `/gift/[id]` 增加 `from=preview` 承接标记。
- 预览来源的礼物效果页显示“礼物效果预览 / 返回预览”，便于回到预览页继续操作。
- 分享出去的 `/gift/[id]` 增加右下角转发按钮，可复制当前礼物链接继续传播。
- 二次转发链接只进入礼物效果页，支持继续转发，不提供编辑入口。
- 修复礼物页喇叭按钮事件隔离，避免静音 / 播放点击被礼物触摸层误处理。
- 抽出共享 `GiftBackground` 背景层，内容页图片预览和最终礼物页使用同一套背景位置、缩放、模糊、蒙版与氛围层规则。
- 内容页上传图片预览框文案调整为 `图片预览栏`，并显示更接近最终 H5 背景的裁切范围。

## 阶段 2 分享体验修正今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已通过本地 HTTP 验证 `/create/preview` 返回 200。
- 已通过本地 HTTP 验证 `/gift/demo?from=preview` 返回 200。
- 已验证未配置 Supabase 时 `/api/gifts/no-such-gift` 返回 503，前端可继续走本地 IndexedDB 兜底。
- 按阶段 2 验收清单复核：本轮只调整分享、礼物效果承接、喇叭按钮事件和图片背景预览一致性，不改变选歌、上传、内容编辑、预览和收礼主流程。

## 阶段 2 分享体验修正待办事项

- 用户在浏览器中手动确认：预览页点击“礼物效果”后能看到返回预览入口。
- 用户在浏览器中手动确认：分享出去的 `/gift/[id]` 可复制链接继续转发，且没有编辑入口。
- 用户上传一张图片后手动确认：内容页图片预览栏与最终礼物背景可见范围是否符合预期。

## 阶段 2 分享体验修正风险 / 阻塞

- 不同手机屏幕比例下，最终整屏 H5 的图片裁切仍会随视口比例略有变化；当前已保证同一设备 / 同一浏览器中预览和最终页使用同构背景算法。
- 当前环境未提供 Supabase 凭证，真实跨设备云端链路仍需配置后验收。

## 阶段 2 分享体验修正下一步计划

- 等待用户体验确认本轮 1-4 项是否通过。
- 若背景预览仍需更精确，可继续把内容页预览框高度改为读取当前设备可视高度，以进一步模拟最终礼物页比例。

---

## 阶段 2 文案与预览比例修正今日完成

- 确认首页“开始制作”入口指向 `/create/song`，即创作流程第一步。
- 新增 `/create` 路由，并自动重定向到 `/create/song`，确保泛化创作链接也进入第一步。
- 删除分享弹窗中的 Supabase / IndexedDB / 本机验收等技术文案，云端失败时仍生成本地可复制链接，但只向用户展示友好提示。
- 将礼物效果页预览返回入口改为只显示 `返回` 两字。
- 将预览返回入口移到右上角，降低视觉存在感，避免遮挡主要礼物画面和底部按钮。
- 删除二次转发弹窗中的“打开后只能查看礼物效果和继续转发”说明。
- 将内容页上传图片预览框从固定 9:16 调整为更接近当前 H5 可视高度的瘦长比例，减少“预览比最终宽很多”的错觉。

## 阶段 2 文案与预览比例修正今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/create` 返回 307 并跳转到 `/create/song`。
- 已验证 `/create/preview` 返回 200。
- 已验证 `/gift/demo?from=preview` 返回 200。

## 阶段 2 文案与预览比例修正待办事项

- 用户刷新浏览器后确认分享弹窗不再出现技术术语。
- 用户确认内容页图片预览栏比例是否更接近最终 H5 礼物背景。

## 阶段 2 文案与预览比例修正风险 / 阻塞

- 图片预览框已更接近 H5 整屏比例，但不同设备高度仍会造成轻微裁切差异。
- 当前仍未配置 Supabase，跨设备真实云端分享需要后续填入环境变量后验收。

## 阶段 2 文案与预览比例修正下一步计划

- 等待用户按页面实际效果确认本轮 5 项修正是否通过。

---

## 阶段 2 图片控制与入口修正今日完成

- 根据用户反馈，将 `/create` 从进入选歌页改为回到最开始的引导首页 `/`。
- 保持图片预览栏 9:16 手机比例，但缩小整体显示面积，避免在内容页占比过大。
- 将图片缩放控件文案从 `Zoom in` 改为 `比例`。
- 图片比例改为 50% - 220%，其中 100% 表示不改变默认大小，低于 100% 为缩小，高于 100% 为放大。
- 上下位置滑杆不再因比例为 100% 被禁用，上传图片后始终可以上下挪动。

## 阶段 2 图片控制与入口修正今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/create` 返回 307 并跳转到 `/`。
- 已验证 `/create/content` 返回 200。
- 已确认代码中不再显示 `Zoom in` 文案。

## 阶段 2 图片控制与入口修正待办事项

- 用户刷新内容页后确认图片预览栏大小是否合适。
- 用户上传图片后确认 `比例` 小于 100%、等于 100%、大于 100% 三种状态都符合预期。

## 阶段 2 图片控制与入口修正风险 / 阻塞

- 比例低于 100% 时，图片可能无法覆盖整个礼物背景，边缘会露出主题底色；这是允许缩小后的自然表现。

## 阶段 2 图片控制与入口修正下一步计划

- 等待用户按页面实际效果确认入口、预览框大小和图片比例控制是否通过。

---

## 阶段 2 祝福颜色设置今日完成

- 在内容页新增 `祝福颜色` 设置。
- 支持通过原生颜色选择器选择弹幕 / 祝福文字颜色。
- 新增常用颜色色块，可一键切换祝福颜色。
- 新增 `吸取颜色` 按钮；浏览器支持 EyeDropper 时可从页面取色，不支持时提示使用颜色框选择。
- 颜色选择会写入草稿，并同步影响预览页和礼物页祝福文字颜色。

## 阶段 2 祝福颜色设置今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。

## 阶段 2 祝福颜色设置待办事项

- 用户在浏览器中手动确认颜色框、色块和吸取颜色是否符合使用预期。
- 用户确认颜色变化能在预览礼物和最终礼物效果页同步展示。

## 阶段 2 祝福颜色设置风险 / 阻塞

- EyeDropper API 不是所有浏览器都支持；不支持时仍可使用颜色选择器和色块。

## 阶段 2 祝福颜色设置下一步计划

- 等待用户按页面实际效果确认弹幕 / 祝福颜色设置是否通过。

---

## 阶段 2 引导页与草稿重置修正今日完成

- 引导首页只保留一个 `开始制作` 按钮，删除 `查看示例` 按钮。
- 打开或刷新引导首页时会清空旧创作草稿，让用户重新开始流程。
- 点击 `开始制作` 时再次清空草稿，避免旧图片或旧音频被带入新流程。
- 内容页每次进入时将背景图片预览恢复为空白 `图片预览栏`，上传图片后才显示图片。
- 将颜色设置中的 `吸取颜色` 改为 `更多颜色`。
- `更多颜色` 不再调用吸管取色，而是打开左侧颜色框，避免操作引导歧义。

## 阶段 2 引导页与草稿重置修正今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证首页 `/` 返回 200。
- 已验证内容页 `/create/content` 返回 200。
- 已确认代码中不再出现 `查看示例`、`吸取颜色`、`EyeDropper` 文案或逻辑。

## 阶段 2 引导页与草稿重置修正待办事项

- 用户刷新首页后确认只看到一个 `开始制作` 按钮。
- 用户点击 `开始制作` 后确认旧背景图不会自动带入内容页。
- 用户确认点击 `更多颜色` 能打开颜色选择器。

## 阶段 2 引导页与草稿重置修正风险 / 阻塞

- 现在每次从引导页重新开始都会清空旧草稿；如果用户误回首页，未生成链接的草稿会被重置。

## 阶段 2 引导页与草稿重置修正下一步计划

- 等待用户按页面实际效果确认本轮 3 项修正是否通过。

---

## 阶段 3 音乐链接与推荐 mock 今日完成

- 在选歌页开放 `QQ 音乐 / 酷狗链接` 输入框和识别按钮。
- 新增本地 mock 音乐库，支持识别 QQ 音乐与酷狗链接形态。
- 粘贴可识别链接后，展示 mock 歌曲名、歌手、平台和封面色块，并写入礼物草稿。
- 识别失败时给出明确提示，并保留上传音频、搜索推荐和系统 BGM 兜底路径。
- 新增搜索推荐输入框，支持按歌名、歌手、平台和场景关键词筛选内置推荐。
- 真实音乐 API 不可用时默认展示内置推荐列表，点击推荐后可试听 mock 音色并继续主流程。
- 扩展 `songSourceType`，区分上传音频、系统 BGM、链接解析和搜索推荐来源。

## 阶段 3 音乐链接与推荐 mock 今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已启动本地 dev server，并验证以下路由均返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 按阶段 3 验收清单复核：本轮覆盖链接平台识别、mock 解析成功展示、失败提示、搜索推荐、API 不可用时内置推荐兜底，以及失败后继续选择系统 BGM。
- 本轮只修改选歌音乐能力，不改变上传音频、内容编辑、预览、生成链接和收礼互动主流程。

## 阶段 3 音乐链接与推荐 mock 待办事项

- 用户在浏览器中手动确认 QQ 音乐 / 酷狗示例链接识别展示是否符合预期。
- 用户手动确认搜索关键词筛选和点击推荐后的试听体验。
- 后续阶段 3 可继续补真实 API 适配层或更多 mock 示例链接。

## 阶段 3 音乐链接与推荐 mock 风险 / 阻塞

- 当前仍不接真实 QQ 音乐 / 酷狗 API，解析结果为本地 mock，适合阶段 3 兜底验收。
- mock 推荐没有真实播放地址，礼物页仍使用现有合成 BGM 播放兜底，但保留用户选择的歌曲标题和歌手信息。

## 阶段 3 音乐链接与推荐 mock 下一步计划

- 在用户确认该模块体验通过后，再继续阶段 3 的真实 API 适配或更完整的失败态打磨。

---

## 阶段 3 选歌页四方式互斥交互今日完成

- 将选歌页音乐来源整理为四种互斥方式：
  - 方式一：QQ 音乐 / 酷狗链接。
  - 方式二：歌曲推荐。
  - 方式三：上传本地音频。
  - 方式四：系统 BGM。
- 页面顶部新增 `当前选择`，明确展示最终会生效的背景歌曲。
- 链接识别成功后不再自动选中歌曲，只展示待使用歌曲卡片，用户需要点击 `使用这首歌` 才会写入草稿。
- 歌曲推荐默认不展开，只有输入关键词后才展示推荐结果。
- 歌曲推荐最多展示 3 首最相关的 mock 曲目。
- 将 `搜索推荐` 改为 `歌曲推荐`，并优化输入框和兜底提示文案。
- 上传本地音频说明去掉阶段描述，只保留格式和 15MB 文件大小限制。
- 将真实 QQ / 酷狗 API、第三方音乐 API 接入、系统 BGM 编辑记录为阶段 3 后续技术路线，并更新项目文档。

## 阶段 3 选歌页四方式互斥交互今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已通过本地 HTTP 验证以下路由均返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 按阶段 3 验收清单复核：本轮仍保留链接识别、mock 成功展示、失败提示、歌曲推荐、API 不可用兜底和系统 BGM 兜底。
- 本轮只调整选歌页交互和阶段 3 技术路线文档，不改变内容编辑、预览、生成链接和收礼互动主流程。

## 阶段 3 选歌页四方式互斥交互待办事项

- 用户在浏览器中确认：链接识别成功后，未点击 `使用这首歌` 前不会覆盖当前选择。
- 用户在浏览器中确认：歌曲推荐默认不显示列表，输入关键词后最多展示 3 首。
- 后续可继续接入真实音乐 API 适配层或系统 BGM 编辑模块。

## 阶段 3 选歌页四方式互斥交互风险 / 阻塞

- 当前链接识别和推荐仍为 mock；真实 QQ / 酷狗播放能力受 API、版权、登录态或播放地址稳定性限制，必须保留上传音频和系统 BGM 兜底。
- mock 推荐没有真实播放地址，礼物页仍使用现有合成 BGM 播放兜底，但保留用户选择的歌曲标题和歌手信息。

## 阶段 3 选歌页四方式互斥交互下一步计划

- 等待用户确认选歌页交互是否通过。
- 若通过，可继续阶段 3 的真实音乐 API 适配层设计或系统 BGM 编辑模块。

---

## 阶段 3 创作入口与资源重置规则今日完成

- 新增创作流程 session 标记，用于区分“重新打开创作链接”和“当前流程内继续编辑”。
- 直接打开 `/create/song`、`/create/content`、`/create/preview` 且不在当前创作流程内时，会回到引导页 `/`。
- 刷新选歌页、内容页或预览页时，会清空上一次上传的音频和图片资源，避免旧上传内容自动带入新体验。
- 从交互礼物页右上角 `返回` 回到预览 / 编辑链路时，会保留用户最近一次上传的音频和图片记录。
- 选歌页 `当前选择` 默认显示空白提示，只有用户明确选择链接歌曲、推荐歌曲、上传音频或系统 BGM 后才展示歌曲信息。
- 链接识别结果与当前选择解耦：选择推荐、本地音频或系统 BGM 不会清掉方式一的识别卡片。
- 选择新歌曲或上传本地音频时，会停止上一段试听，避免多个试听叠加。

## 阶段 3 创作入口与资源重置规则今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已重新启动本地 dev server，并验证以下路由均返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 本轮只调整创作入口、草稿资源重置和选歌当前选择状态，不改变内容编辑表单、预览生成链接和收礼互动主流程。

## 阶段 3 创作入口与资源重置规则待办事项

- 用户在浏览器中手动确认：新开创作链接会回到引导页。
- 用户在浏览器中手动确认：刷新选歌 / 内容 / 预览页后，上一次上传的音频和图片不会继续显示或播放。
- 用户在浏览器中手动确认：从礼物效果页返回编辑链路时，最近一次上传记录仍被保留。

## 阶段 3 创作入口与资源重置规则风险 / 阻塞

- 浏览器刷新、直接打开链接和应用内路由跳转依赖浏览器 navigation 与 sessionStorage 行为；不同浏览器可能有轻微差异，需要移动端真机再验收。
- 文件上传控件本身出于浏览器安全策略无法程序化保留文件选择框文本，但草稿中的 Data URL 资源会按规则保留或清空。

## 阶段 3 创作入口与资源重置规则下一步计划

- 等待用户按浏览器实际效果确认 4 条交互规则是否通过。

---

## 阶段 3 选歌试听与本地音频回选今日完成

- 将选歌页系统 BGM 和 mock 歌曲试听时长统一调整为 10 秒。
- 新试听开始前会停止上一段试听，避免多个 BGM 或推荐曲目叠加播放。
- 上传本地音频后会保留一张已上传音频卡片。
- 用户试听其他系统 BGM 或推荐曲目后，可在方式三点击 `使用该音乐`，重新选回已上传的本地音频。
- 选回本地音频时会更新 `当前选择`，并把上传音频重新写入礼物草稿。

## 阶段 3 选歌试听与本地音频回选今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已确认本地 dev server 可访问，并验证：
  - `/`
  - `/create/song`
- 本轮只调整选歌页试听时长、本地音频回选和对应样式，不改变内容页、预览页、分享和收礼互动主流程。

## 阶段 3 选歌试听与本地音频回选待办事项

- 用户在浏览器中手动确认：系统 BGM / 推荐曲目试听约 10 秒后停止。
- 用户上传本地音频后，先试听其他 BGM，再点击方式三 `使用该音乐`，确认当前选择回到本地音频。

## 阶段 3 选歌试听与本地音频回选风险 / 阻塞

- 浏览器可能因自动播放策略要求用户先点击页面；当前按钮点击触发的试听路径可用，若浏览器阻止播放会保留当前选择并提示需要用户交互。

## 阶段 3 选歌试听与本地音频回选下一步计划

- 等待用户确认本地音频回选体验是否符合预期。

---

## 阶段 3 交互页引导后播放今日完成

- 交互礼物页不再在进入页面时立即尝试播放音乐。
- 手势引导蒙层不再 3 秒后自动消失，必须由用户点击 `我知道了` 关闭。
- 用户点击 `我知道了` 后，音乐按钮才开始尝试播放背景音乐。
- 若浏览器仍拦截播放，喇叭按钮保留点击播放兜底。

## 阶段 3 交互页引导后播放今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证交互相关路由返回 200：
  - `/create/preview`
  - `/gift/demo`
- 本轮只调整交互页音乐启动时机和引导蒙层关闭方式，不改变花朵触摸、分享按钮、内容编辑和生成链接主流程。

## 阶段 3 交互页引导后播放待办事项

- 用户在浏览器中手动确认：引导蒙层出现时不播放音乐。
- 用户在浏览器中手动确认：点击 `我知道了` 后才开始播放或进入可点击播放状态。

## 阶段 3 交互页引导后播放风险 / 阻塞

- 移动浏览器仍可能拦截音频播放；当前 `我知道了` 属于用户手势触发，成功率更高，但仍保留喇叭按钮手动播放兜底。

---

## 阶段 3 选歌页文案微调今日完成

- 将选歌页标题下说明文案从解释互斥规则，改为更直接的用户引导：`可以通过下列四种方式选一首最想送给 TA 的歌。`

## 阶段 3 选歌页文案微调今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/create/song` 返回 200。

---

## 阶段 3 选歌页批注文案与上传控件今日完成

- 将选歌页主标题改为 `将音乐卡带放进你的礼物盒~`。
- 移除 QQ / 酷狗链接区域中面向开发验证的 mock 示例链接展示。
- 将方式四标题简化为 `方式四：系统 BGM`。
- 将本地音频原生文件输入替换为淡粉色上传引导框。
- 未上传前上传框显示 `点击上传本地音乐文件`，上传后显示文件名。

## 阶段 3 选歌页批注文案与上传控件今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/create/song` 返回 200。

---

## 阶段 3 内容页批注文案今日完成

- 将内容页主标题改为 `花之物语：把心声装进礼物`。
- 将标题下说明改为 `创造你的个性化礼物`。
- 将称呼字段标签改为 `心里的TA（对方称呼）`。
- 将称呼字段占位和默认值从 `XX` 改为 `TA`。
- 将称呼提示改为 `最多15字，不填时默认为TA。`

## 阶段 3 内容页批注文案今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/create/content` 返回 200。

---

## 阶段 3 音乐 API 适配层今日完成

- 新增 `POST /api/music/parse-link`，先以现有本地 mock 能力承接 QQ 音乐 / 酷狗链接识别。
- 新增 `POST /api/music/search`，先以现有本地 mock 音乐库承接歌曲推荐搜索，最多返回 3 首。
- 选歌页链接识别与歌曲推荐改为优先调用 API；API 不可用时自动回退到本地 mock 函数。
- 保持四种选歌方式互斥规则不变，不改动上传音频、系统 BGM、内容编辑、预览生成链接和收礼互动主流程。
- 更新 `docs/06-api-and-data-spec.md`，记录当前音乐 API 适配层实现状态。

## 阶段 3 音乐 API 适配层今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `POST /api/music/parse-link` 可返回 QQ 音乐 mock 解析成功结果。
- 已验证 `POST /api/music/parse-link` 对非音乐链接返回失败提示和 `fallback: "search"`。
- 已验证 `POST /api/music/search` 可按关键词返回最多 3 首内置推荐。
- 已验证主要路由仍返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`

## 阶段 3 音乐 API 适配层待办事项

- 用户在浏览器中确认：链接识别与歌曲推荐体验与上一版一致。
- 后续如接真实 QQ 音乐 / 酷狗或第三方音乐 API，优先替换这两个 API 路由内部实现。

## 阶段 3 音乐 API 适配层风险 / 阻塞

- 当前仍是 mock 数据，不提供真实播放地址；礼物页继续使用上传音频或系统 BGM 兜底。
- 真实音乐 API 可能受版权、登录态、限流或签名限制，接入时必须保留当前兜底路径。

## 阶段 3 音乐 API 适配层下一步计划

- 在用户确认该模块体验通过后，再继续阶段 3 的真实音乐服务接入评估或系统 BGM 编辑模块。

---

## 阶段 4 摄像头授权与触摸兜底今日完成

- 在交互礼物页手势引导蒙层中新增 `启用摄像头手势` 入口。
- 调用浏览器 `getUserMedia` 请求前置摄像头权限，只请求视频，不请求音频。
- 同意权限后将数据面板模式更新为 `摄像头`，并保留现有花园交互画面。
- 摄像头视频流仅作为本机实时输入，隐藏在页面外，不保存、不上传。
- 浏览器不支持摄像头或用户拒绝授权时，自动回到 `触摸` 模式并给出明确提示。
- 组件卸载时会停止摄像头 tracks，避免离开礼物页后继续占用摄像头。
- 左上角数据面板新增 `模式` 字段，显示当前为 `摄像头` 或 `触摸`。
- 根据用户反馈修正引导逻辑：摄像头只由按钮点击触发；授权成功后引导卡切换为摄像头手势说明；授权失败或唤起失败时保持触摸模式，并显示触摸教程。
- 预览页和 `/gift/[id]` 礼物页继续共用 `GiftExperience`，创作者预览交互界面和收礼人礼物页保持一致的手势引导逻辑。
- 根据用户反馈将引导层拆成两块：第一块优先引导 `舞动双手，唤醒这片花园` 并直接提供 `点击启动摄像头` 按钮；第二块说明无法唤起摄像头时可用触摸完成交互。
- 摄像头启动后，页面会展示实时摄像头预览，便于用户理解镜头里的情况；关闭引导后舞台右上角仍保留小预览。
- 摄像头手势教学拆成 5 个板块：手掌上移、手掌下移、左右移动、张开五指、快速摆手，避免使用容易误解的 `握拳 / 合掌` 文案。
- 根据用户反馈将摄像头手势教学改为 4 个板块：手掌上下摆动、手掌左右摇晃、张开五指并拢双拳、拇指与食指孔雀形状捏合。
- 增强摄像头启动稳定性：按钮点击后显示启动中，先请求前置摄像头，失败后自动降级请求任意可用摄像头。

## 阶段 4 摄像头授权与触摸兜底今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证主要路由仍返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 已在当前浏览器页面读取 `/gift/demo` 可见文本，确认初始引导包含 `启用摄像头手势` 和触摸教程。
- 已在当前浏览器页面读取 `/gift/demo` 可见文本，确认引导包含 `舞动双手，唤醒这片花园`、`点击启动摄像头` 和触摸兜底说明。
- 已确认代码中不再出现 `握拳 / 合掌` 摄像头教学文案。
- 已确认摄像头教学文案更新为 4 个板块，并再次运行 `npm.cmd run build` 通过。
- 已验证 `/gift/demo` 和 `/create/preview` 返回 200。
- 按阶段 4 验收清单复核：本轮覆盖摄像头授权引导、同意权限后进入摄像头模式、摄像头画面不保存不上传、摄像头失败自动进入触摸模式。

## 阶段 4 摄像头授权与触摸兜底待办事项

- 接入 MediaPipe Hands，基于摄像头画面识别基础手势。
- 将识别到的手势映射到高度、风力、开合和换色，并持续更新数据面板。
- 在真机浏览器中手动确认摄像头授权、拒绝授权和返回触摸模式的实际体验。

## 阶段 4 摄像头授权与触摸兜底风险 / 阻塞

- 当时只完成摄像头授权和模式切换；后续同日已接入 MediaPipe HandLandmarker 基础识别。
- 摄像头权限依赖浏览器和安全上下文；非 localhost 或非 HTTPS 环境可能无法授权。
- 摄像头唤起稳定性仍受浏览器权限状态、设备占用、摄像头驱动和站点权限缓存影响；当前已增加前置摄像头失败后的普通摄像头降级重试。
- 不同移动浏览器对前置摄像头、自动播放和权限弹窗行为可能不一致，需要真机验收。

## 阶段 4 摄像头授权与触摸兜底下一步计划

- 下一模块接入 MediaPipe Hands，先识别基础手势状态，再把识别结果接入现有花朵交互状态。

---

## 阶段 4 MediaPipe 基础手势识别今日完成

- 安装 `@mediapipe/tasks-vision`，接入官方 MediaPipe HandLandmarker。
- 摄像头启动成功后加载 HandLandmarker 模型，并开始逐帧识别摄像头视频。
- 使用手部 21 个关键点进行基础交互映射：
  - 掌心上下位置 -> 花朵高度。
  - 掌心左右位置 / 移动 -> 风力与左右摇晃。
  - 手指伸展数量 -> 花朵张开 / 闭合。
  - 拇指与食指捏合距离 -> 触发统一换色。
- 手势识别结果会更新左上角数据面板的模式、手势、高度和风力。
- MediaPipe 或摄像头初始化失败时，仍自动保持触摸模式。
- 组件卸载时会停止摄像头 tracks，并关闭 HandLandmarker。

## 阶段 4 MediaPipe 基础手势识别今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 已验证 `/gift/demo` 返回 200。
- 已验证 `/create/preview` 返回 200。
- 本轮只接入摄像头识别与映射，不改动选歌、内容编辑、分享生成、上传资源和礼物读取主流程。

## 阶段 4 MediaPipe 基础手势识别待办事项

- 用户在浏览器中授权摄像头后，手动验证四类手势映射是否符合直觉。
- 根据真机体验微调关键点阈值，例如捏合距离、张开 / 闭合判定、上下和左右灵敏度。
- 后续可增加摄像头识别加载态或失败细分提示。

## 阶段 4 MediaPipe 基础手势识别风险 / 阻塞

- HandLandmarker 模型和 wasm 运行时需要浏览器访问外部 CDN / Google 模型地址；离线或网络受限时会回退触摸模式。
- 摄像头权限、设备占用和浏览器策略仍可能影响手势模式稳定性。
- 当前识别是基础启发式映射，适合阶段 4 验收，仍需要真机调参。

## 阶段 4 MediaPipe 基础手势识别下一步计划

- 根据用户真机验收反馈，微调手势识别阈值和交互灵敏度。

---

## 阶段 4 摄像头手势体验稳定性优化今日完成

- 将摄像头启动改成两段式：先显示摄像头预览，再后台准备 MediaPipe 手势识别。
- 摄像头启动文案从 `正在启动摄像头...` 细化为摄像头启动和手势识别准备状态。
- 在引导页摄像头预览和关闭引导后的右上角小预览中加入彩色实线轨迹。
- 轨迹使用食指指尖最近约 1.2 秒的位置绘制，并带有尾迹和当前追踪点，帮助用户感知手势被识别。
- 增加单手主控策略：多手进入画面时，只选择离画面中心最近的一只手作为控制手。
- 增加掌心坐标平滑，降低摄像头抖动造成的高度 / 风力跳变。
- 增加移动死区，减少微小抖动导致的误触发。
- 增加开合防抖：张开 / 闭合候选状态稳定约 300ms 后再切换。
- 保留换色冷却，避免捏合动作连续触发造成卡顿。
- 更新摄像头手势说明，补充音量和风向随手势变化：
  - 手掌上下摆动：花朵长高变矮；越高声音越大，越矮声音越小。
  - 手掌左右摇晃：花朵左右摇晃；越靠左声音越小，越靠右声音越大，并改变西风 / 东风。
- 在摄像头教程中增加单手居中提示：建议只伸出一只手，并尽量保持在画面中央。

## 阶段 4 摄像头手势体验稳定性优化今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 本轮只优化摄像头启动、轨迹反馈和识别稳定性，不改动选歌、内容编辑、分享生成和触摸兜底主流程。

## 阶段 4 摄像头手势体验稳定性优化待办事项

- 用户在真机或带摄像头浏览器中确认摄像头画面是否明显更快出现。
- 用户确认彩色轨迹是否能帮助理解识别状态。
- 根据实际体验继续微调单手主控、平滑参数、死区和开合防抖时长。

## 阶段 4 摄像头手势体验稳定性优化风险 / 阻塞

- 摄像头启动速度仍受浏览器权限、设备占用和硬件驱动影响；两段式启动只能减少模型加载造成的等待感。
- 多手场景当前按“离画面中心最近”选择主控手，复杂多人场景仍建议用文案引导用户只伸出一只手。

## 阶段 4 摄像头手势体验稳定性优化下一步计划

- 等待用户根据实际摄像头体验反馈，再决定是否进一步微调阈值或进入阶段 5 体验打磨。

---

## 阶段 4 手掌姿态识别优化今日完成

- 将摄像头可视化从单点轨迹升级为整手可视化：
  - 绘制手掌轮廓。
  - 绘制 21 个关键点构成的手指骨架。
  - 高亮拇指尖和食指尖，便于理解捏合换色。
  - 显示掌心主控点和掌心轨迹。
- 将多手选择策略改为右手优先、左手候补：
  - 有右手时只用右手控制。
  - 没有右手时使用左手。
  - 无 handedness 信息时，回退到离画面中心最近的手。
- 将高度控制从掌心 XY 位置改为手掌姿态：
  - 使用手腕、中指根、食指根和小指根估算手掌上翘 / 放下。
  - 手掌上翘 / 放下控制花朵长高变矮。
- 将风力控制改为手掌左右倾斜 + 轻量位置辅助，减少上下和左右手势互相干扰。
- 将开合识别改为整手伸展状态，基于指尖到掌心的归一化距离判断张开 / 收拢。
- 将换色识别改为归一化捏合距离：拇指尖与食指尖距离 / 掌宽，降低远近变化对阈值的影响。
- 调快手势响应：
  - 掌心平滑从偏慢改为更快响应。
  - 开合稳定时间从约 300ms 降到约 200ms。
  - 移动死区略降低。
  - 换色冷却保留约 900ms。
- 更新摄像头引导文案：`手掌上翘 / 放下` 控制高低，`手掌左右摇晃` 控制风向和音量。

## 阶段 4 手掌姿态识别优化今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建通过。
- 本轮只优化摄像头识别与可视化，不改动选歌、内容编辑、上传、分享生成和触摸兜底主流程。

## 阶段 4 手掌姿态识别优化待办事项

- 用户用真实摄像头确认右手优先、左手候补是否符合预期。
- 用户确认手掌上翘 / 放下控制高低是否比掌心 XY 更自然。
- 根据实际体验继续微调掌心姿态、风力倾斜、开合伸展和捏合阈值。

## 阶段 4 手掌姿态识别优化风险 / 阻塞

- MediaPipe handedness 在镜像画面和不同摄像头下可能存在左右判断偏差，需要真机确认。
- 手掌姿态基于 2D / z 值启发式估算，不同手型和摄像头角度可能需要调参。

## 阶段 4 手掌姿态识别优化下一步计划

- 等待用户验收后，根据具体问题调整阈值或进入阶段 5 打磨。

---

## 阶段 4 双手入镜与手掌角度范围优化今日完成

- 将 MediaPipe HandLandmarker 的识别数量调整为最多 2 只手。
- 双手同时入镜时，仍按右手优先、左手候补选择主控手。
- 主控手继续使用彩色实线整手骨架、手掌轮廓、掌心点和掌心轨迹。
- 非主控手仅显示白色虚线整手骨架和虚线轨迹，不参与花朵高度、风力、开合或换色控制。
- 没有右手时，左手会自动成为主控手；无 handedness 信息时仍回退为离画面中心最近的手。
- 放宽手掌上翘 / 放下的高度映射范围，提高小幅度掌面角度变化对花朵高度的响应。

## 阶段 4 双手入镜与手掌角度范围优化今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 本轮只调整摄像头手势识别、可视化和阈值，不改动选歌、内容编辑、上传、分享生成、礼物读取和触摸兜底主流程。

## 阶段 4 双手入镜与手掌角度范围优化待办事项

- 用户用真实摄像头验证：右手入镜时右手为实线主控，左手为虚线非主控。
- 用户验证：移出右手后，左手应自动切换为实线主控并继续控制花朵。
- 用户验证：手掌上翘 / 放下的高度变化范围是否比上一版更容易触发。

## 阶段 4 双手入镜与手掌角度范围优化风险 / 阻塞

- handedness 仍依赖 MediaPipe 在具体摄像头和镜像画面下的判断，移动端真机可能需要继续调参。
- 手掌角度仍是基于 2D 关键点和 z 值的启发式映射，不同手型、距离和光照下可能表现不同。

## 阶段 4 双手入镜与手掌角度范围优化下一步计划

- 等待用户真机验收双手虚线、右手优先 / 左手候补和手掌上翘 / 放下灵敏度。
- 若验收通过，再进入阶段 5 体验打磨；若仍有识别偏差，继续只在阶段 4 内微调阈值和提示文案。

---

## 阶段 4 双手融合、捏合换色与模式切换今日完成

- 根据用户验收反馈，取消左右手识别样式差异：两只手都使用同样的彩色实线骨架、手掌轮廓、拇指 / 食指点和掌心轨迹。
- 将横向控制从单一 X 位移升级为复合信号：
  - 掌心横向移动速度。
  - 手掌左右倾斜。
  - 指尖相对掌心的横向摆动。
- 摄像头横向坐标按用户看到的镜像预览统一，左右手向屏幕同一方向移动时，内部风向信号保持一致。
- 双手同时入镜时，若动作方向和高度接近，则融合两只手的信号；若路径差异较大，则按右手优先 / 左手候补策略控制。
- 将花朵长高 / 变矮改为掌心 Y 位置主控：掌心上移花朵长高，掌心下移花朵变矮。
- 将捏合换色改为状态机：
  - 放宽捏合进入阈值，提高轻捏触发概率。
  - 增加释放阈值，松开后再次捏合才允许下一次换色。
  - 将换色冷却缩短到约 580ms，降低换色迟钝感。
  - 双手任一只手捏合即可触发换色，同时捏合只触发一次。
- 新增摄像头 / 触摸模式切换入口：
  - 摄像头模式可切换到触摸模式，并停止摄像头 tracks。
  - 触摸模式可重新启动或回到摄像头手势识别。
  - 创作者预览页和礼物链接页因共用 `GiftExperience` 同步生效。
- 问号引导页根据当前触发形式展示规则：
  - 摄像头模式展示掌心上下、左右复合摇晃、开合、捏合换色说明。
  - 触摸模式展示上下滑、左右滑、点击、双击说明。
  - 首次进入且未授权时仍优先引导启动摄像头，并保留触摸兜底教程。

## 阶段 4 双手融合、捏合换色与模式切换今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 本轮只调整摄像头手势、触摸 / 摄像头模式切换和问号引导，不改动选歌、内容编辑、上传、分享生成和礼物读取主流程。

## 阶段 4 双手融合、捏合换色与模式切换待办事项

- 用户用真实摄像头验证：左右手向屏幕同一方向移动时，花朵风向变化一致。
- 用户验证：原地左右摆手腕或摆指尖时，花朵左右响应是否比上一版明显。
- 用户验证：双手同向时响应更稳定，双手路径差异较大时优先按右手控制。
- 用户验证：轻捏拇指和食指是否更容易触发统一换色，松开后再次捏合可再次换色。
- 用户验证：摄像头模式和触摸模式可互相切换，问号页说明与当前模式一致。

## 阶段 4 双手融合、捏合换色与模式切换风险 / 阻塞

- MediaPipe handedness 在镜像摄像头和部分移动浏览器中仍可能左右判断偏差，需要真机观察。
- 复合左右信号为启发式权重，可能需要根据实际手型、距离、光照继续微调。
- 切换回摄像头时仍依赖浏览器权限、设备占用和模型加载状态。

## 阶段 4 双手融合、捏合换色与模式切换下一步计划

- 等待用户按上述场景验收；若通过，再进入阶段 5 体验打磨。
- 若仍存在识别偏差，优先继续微调复合横向信号权重、融合冲突阈值和捏合阈值。

---

## 阶段 4 手势灵敏度与极端高度容错今日完成

- 根据用户验收反馈，提高摄像头上下 / 左右手势响应灵敏度。
- 收窄掌心 Y 高度映射区间，让掌心小幅上移 / 下移也能带来更明显的花朵高低变化。
- 为高度控制加入垂直速度辅助：掌心连续上移 / 下移时，花朵高度会获得轻量额外响应。
- 优化左右复合信号：
  - 保留掌心横向速度。
  - 保留手掌左右倾斜。
  - 保留指尖相对掌心横摆。
  - 当手处于较高或较低位置时，自动提高左右倾斜和指尖横摆权重，降低对掌心横向位移的依赖。
- 为高位 / 低位左右摇晃增加补偿，使花朵在较高或较低状态下仍能识别左右风力变化。
- 增加单帧最大变化限制，避免灵敏度提升后高度和风力突然大幅跳变。
- 双手融合加入 2 帧稳定缓冲，减少双手融合和右手优先之间频繁切换。
- 增加约 250ms 短暂丢帧保持：手到画面边缘或短暂丢关键点时，不立刻清空手势状态。
- 保留上一轮的双手同向融合、路径差异较大时右手优先、捏合状态机和摄像头 / 触摸模式切换。

## 阶段 4 手势灵敏度与极端高度容错今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 本轮只调整摄像头手势识别参数和容错策略，不改动触摸模式、选歌、内容编辑、上传、分享生成和礼物读取主流程。

## 阶段 4 手势灵敏度与极端高度容错待办事项

- 用户验证：掌心小幅上移 / 下移时，花朵高度是否比上一版更明显。
- 用户验证：掌心小幅左右晃、原地摆手腕或摆指尖时，风力是否更容易触发。
- 用户验证：花朵处于较高或较低位置时，左右晃动是否仍能被识别。
- 用户验证：上下移动时风力不应明显乱跳，左右摆动时高度不应大幅乱变。
- 用户验证：张开 / 收拢和捏合换色不应被左右灵敏度提升明显串扰。

## 阶段 4 手势灵敏度与极端高度容错风险 / 阻塞

- 灵敏度提升可能带来轻微手势串扰，当前用方向判断、单帧变化限制和短暂丢帧保持做了保守控制。
- 高位 / 低位补偿仍是启发式参数，需要用户真机验收后继续微调。

## 阶段 4 手势灵敏度与极端高度容错下一步计划

- 等待用户验收灵敏度和串扰情况；若通过，再进入阶段 5 体验打磨。
- 若仍偏迟钝，优先微调高度映射区间、左右复合信号权重和高低位补偿系数。

---

## 阶段 4 手势灵敏度止血修复今日完成

- 根据用户反馈，修复上一轮增强导致的开合识别异常和指令混乱。
- 修复开合判断坐标 bug：
  - 左右方向控制继续使用用户看到的镜像坐标。
  - 张开 / 握拳的指尖到掌心距离改回使用未镜像的真实掌心坐标，避免张开 / 握拳距离被镜像坐标污染。
- 修复进入摄像头后花朵因手势初始位置低而直接变矮的问题：
  - 摄像头识别首帧会记录当前掌心 Y 和当前花朵高度作为高度基准。
  - 后续高度按相对这个基准的上下移动变化，不再用首帧绝对位置直接压低花朵。
- 降低上一轮过强的灵敏度增益：
  - 降低掌心横向速度增益。
  - 降低垂直速度辅助。
  - 降低高位 / 低位左右补偿强度。
- 增加开合候选变化短窗口保护：张开 / 握拳候选刚变化时，短暂降低高度和风力更新幅度，减少开合动作同时触发上下 / 左右指令。

## 阶段 4 手势灵敏度止血修复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 本轮只修复摄像头手势识别参数和坐标计算，不改动触摸模式、选歌、内容编辑、上传、分享生成和礼物读取主流程。

## 阶段 4 手势灵敏度止血修复待办事项

- 用户验证：张开五指 / 并拢双拳能重新稳定识别花朵开合。
- 用户验证：刚进入摄像头识别时，花朵不会因为手在画面低位而立刻变得很矮。
- 用户验证：左右 / 上下仍有响应，但不再出现明显指令混乱。
- 用户验证：捏合换色、双手融合和摄像头 / 触摸模式切换仍正常。

## 阶段 4 手势灵敏度止血修复风险 / 阻塞

- 当前恢复稳定性优先于极限灵敏度；如果验收后仍觉得偏迟钝，应小步单项调参，避免再次同时放大多个信号源。

## 阶段 4 手势灵敏度止血修复下一步计划

- 等待用户先验收开合、初始高度和指令稳定性。
- 若稳定性恢复，再针对“上下偏迟钝”或“左右偏迟钝”分别单独微调，不再一次性提高所有手势信号。

## 阶段 4-2 保存记录

- 已将阶段 4-2 的关键决策、已完成内容、待办事项、重要文件修改记录和整体架构思路同步到上下文文档：
  - `docs/06-api-and-data-spec.md`
  - `docs/07-development-roadmap.md`
  - `docs/11-session-handoff.md`
- 阶段 4-2 保存重点：
  - MediaPipe HandLandmarker 基础识别。
  - 双手统一可视化与双手同向融合。
  - 路径差异较大时右手优先。
  - 掌心 Y 首帧校准高度基准。
  - 开合真实坐标计算与镜像方向坐标分离。
  - 捏合换色状态机。
  - 摄像头 / 触摸模式手动切换。
  - 手势灵敏度止血修复，恢复稳定性优先。
- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 准备创建 Git 提交并打标签 `stage-4-2`。

## 阶段 4 手势灵敏度与极端高度容错补充验收复核

- 已再次运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 已确认本地 `/gift/demo` 返回 HTTP 200。
- 已通过浏览器 DOM 快照确认 `/gift/demo` 实际渲染数据面板，包含模式、风力、高度、手势状态、问号按钮和启动摄像头按钮。
- 已通过浏览器 DOM 快照确认问号教程可显示触摸规则：上下滑动、左右滑动、点击花朵、双击屏幕。
- 已复核源码：摄像头只通过 `navigator.mediaDevices.getUserMedia` 获取视频流，绑定到本地 video，并用 canvas 绘制识别反馈。
- 已复核源码：`GiftExperience` 内没有 `fetch`、`localStorage`、`indexedDB`、`/api/upload` 或上传调用，摄像头画面不保存、不上传。
- 已复核失败兜底路径：浏览器不支持、摄像头或手势识别失败时都会回到 `mode: "touch"`，并显示触摸兜底提示。
- 未代替用户点击摄像头授权按钮或接受浏览器权限；摄像头真实授权、拒绝授权、真机手势灵敏度仍需用户手动验收。

---

## 阶段 4 摄像头状态与失败提示今日完成

- 在 `GiftExperience` 中新增摄像头阶段状态：未开启、请求权限、准备识别、实时识别、触摸兜底。
- 左上角数据面板新增摄像头状态显示，便于用户确认当前是否处于摄像头 / 识别 / 兜底链路。
- 引导卡新增摄像头状态标签；摄像头实时预览底部会显示“手势识别中”或当前准备状态。
- 将摄像头失败提示细分为权限拒绝、无可用摄像头、设备占用和通用失败。
- 将 MediaPipe 手势识别资源加载失败单独提示，并自动回到触摸模式。
- 本轮只增强阶段 4 状态提示与失败兜底，不调整手势阈值，不改动选歌、内容编辑、上传、分享生成和礼物读取主流程。

## 阶段 4 摄像头状态与失败提示今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 已启动本地 dev server，并验证以下路由返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 已按阶段 4 验收清单复核：摄像头画面仍只绑定本地 video/canvas，不保存、不上传；失败时仍进入触摸模式。

## 阶段 4 摄像头状态与失败提示待办事项

- 用户在真实浏览器中确认：授权成功后状态从请求权限进入准备识别，再进入实时识别。
- 用户在真实浏览器中确认：拒绝权限、摄像头占用或模型加载失败时，页面提示清楚且可继续触摸互动。
- 用户继续验收阶段 4-2 的开合、捏合换色、双手融合、上下 / 左右灵敏度和模式切换。

## 阶段 4 摄像头状态与失败提示风险 / 阻塞

- 当前环境无法代替用户完成真实摄像头授权、拒绝授权或设备占用场景验收。
- MediaPipe wasm 和模型仍依赖外部资源；网络受限时会触发触摸兜底。

## 阶段 4 摄像头状态与失败提示下一步计划

- 等待用户真机确认摄像头状态提示与失败兜底体验。
- 若状态提示通过，再根据用户真机反馈继续在阶段 4 内小步调参；通过后再进入阶段 5 体验打磨。

---

## 阶段 5 分享复制反馈今日完成

- 开始执行阶段 5 的第一个明确模块：分享链接复制反馈优化。
- 新增 `src/lib/clipboard.ts`，优先使用 `navigator.clipboard.writeText`，不可用或失败时回退到隐藏 textarea 复制。
- 预览页分享弹窗复制成功后，按钮短暂显示 `已复制`，提示文案改为“链接已复制，可以直接粘贴转发。”
- 礼物页转发弹窗使用同一套复制兜底和成功反馈。
- 复制失败时明确提示用户长按链接或手动选中后复制。
- 新增成功反馈样式：按钮变为绿色渐变，提示文案加深为绿色。
- 本轮只覆盖阶段 5 的“分享链接复制有反馈”验收项，不改动选歌、内容编辑、上传、礼物读取、摄像头或触摸交互主流程。

## 阶段 5 分享复制反馈今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 已验证以下路由返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 已按阶段 5 验收清单复核：本轮覆盖分享链接复制反馈；移动端整体视觉、加载态、失败态和动画自然度仍需后续模块逐项处理。

## 阶段 5 分享复制反馈待办事项

- 用户在浏览器中手动确认：预览页生成链接后点击复制，按钮短暂显示 `已复制`。
- 用户在浏览器中手动确认：礼物页转发弹窗复制反馈一致。
- 后续阶段 5 继续处理移动端无横向滚动、布局溢出、加载 / 空状态提示和整体动画打磨。

## 阶段 5 分享复制反馈风险 / 阻塞

- 个别浏览器可能限制剪贴板 API；当前已加入 textarea 兜底，但仍需要用户在目标手机浏览器确认。
- 当前没有做全站视觉重构，避免一次性改动过大影响主流程。

## 阶段 5 分享复制反馈下一步计划

- 下一模块建议处理移动端布局溢出与小屏文本适配，优先满足阶段 5 “手机端无横向滚动、遮挡、布局溢出”验收项。

---

## 阶段 5 加载状态补齐今日完成

- 继续执行阶段 5 的第二个明确模块：加载状态补齐。
- 预览页 `/create/preview` 在草稿读取完成前不再空白，改为显示 `正在整理礼物预览` 加载提示。
- 礼物页 `/gift/[id]` 在云端 / 本地礼物读取完成前不再空白，改为显示 `正在打开这份礼物` 加载提示。
- 将预览页和礼物页加载提示设置为最短约 720ms 可见，避免提示一闪而过。
- 新增轻量加载点动画和居中加载卡片样式。
- 本轮只覆盖阶段 5 的“加载状态有清晰提示”验收项，不改动选歌、内容编辑、上传、分享生成、礼物读取策略、摄像头或触摸交互主流程。

## 阶段 5 加载状态补齐今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 已验证以下路由返回 200：
  - `/`
  - `/create/song`
  - `/create/content`
  - `/create/preview`
  - `/gift/demo`
- 延长加载提示后已再次运行 `npm.cmd run build` 并通过，主要路由仍返回 200。
- 已按阶段 5 验收清单复核：本轮覆盖加载态提示；失败态、空状态、移动端布局和动画自然度仍需后续模块继续处理。

## 阶段 5 加载状态补齐待办事项

- 用户手动刷新 `/create/preview`，确认加载提示出现后能进入礼物预览。
- 用户手动打开 `/gift/demo` 或已生成的 `/gift/[id]`，确认加载提示出现后能进入礼物效果页。
- 后续继续处理移动端布局溢出与小屏文本适配。

## 阶段 5 加载状态补齐风险 / 阻塞

- 本地数据读取通常很快，加载提示可能只闪现；慢设备或云端读取时更容易看到。
- 当前没有改变礼物读取失败页面文案，失败态优化可作为阶段 5 后续独立模块。

## 阶段 5 加载状态补齐下一步计划

- 下一模块处理小屏布局与横向溢出，优先检查分享弹窗、底部按钮、数据面板、引导卡和礼物舞台在窄屏下的可读性。
