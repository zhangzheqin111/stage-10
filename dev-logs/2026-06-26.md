# BloomBeat 开发日志 - 2026-06-26

## 今日完成

- 继续阶段 7 数据流 / UI 阻塞修复模块，没有开启新阶段。
- 修复 `/gift/[id]` 礼物页的 React Hook 调用顺序风险：`audioHint` 自动隐藏的 `useEffect` 移到所有条件返回之前，避免礼物加载状态变化后 Hook 顺序不一致。
- 校准自包含分享链接解码兜底：`giftCodec` 在解码缺失字段时，字体大小、祝福流速、祝福占比和行距统一回落到 `defaultGift` 默认值，避免异常分享数据把页面撑坏或动画参数失真。

## 今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 按 `docs/08-acceptance-checklist.md` 做本模块静态验收：
  - 不改变选歌、上传、内容编辑、预览、摄像头或触摸交互主流程。
  - `/gift/share#bloombeat=...` 仍优先从 hash 解码礼物数据。
  - `/gift/demo` 仍从本地草稿读取，便于创作者预览。
  - 礼物页加载、缺失页和正常互动页的 Hook 调用顺序保持稳定。

## 待办事项

- 用户在手机端重新走一遍：首页开始制作 -> 选歌 -> 内容页 -> 预览 -> 礼物效果，确认仍不再卡在整理页。
- 用户用自包含分享链接在另一台设备打开，确认文字、背景、主题和默认 BGM 能正常渲染。

## 风险 / 阻塞

- 自包含分享链接不会携带上传音频或 data URL 图片；这类资源仍需后续 Supabase 配置才能可靠跨设备分享。
- 当前仍需要真机确认移动端浏览器对 URL hash 长度、Web Share API 和本地音频播放策略的兼容性。

## 下一步计划

- 若用户确认阶段 7 数据流修复稳定，下一步按路线图继续处理阶段 2 的 Supabase 跨设备分享配置，或回到阶段 4 的 HTTPS 真机摄像头手势调参。

---

## 阶段 7 体验细节回归修复今日完成

- 继续处理当前链接验收中暴露的制作流程体验问题，未开启新阶段。
- 将本地音频上传限制从 2MB 放宽到 10MB，并同步更新选歌页提示和错误文案。
- 将背景图片上传限制从 100KB 放宽到 3MB，并同步更新内容页提示和错误文案。
- 修复内容页“心里的TA”称呼输入框无法完全删除的问题：输入框现在允许清空，礼物标题预览仍会在空值时兜底为 `TA`。
- 内容页表单区域新增当前主题 CSS 变量，祝福字号、流动速度、行距等 range 拖动条会跟随所选主题色变化。
- 将礼物页左下角音乐按钮从单纯播放 / 静音切换调整为音量入口：点击后显示音量滑块，并保留“暂停音乐”操作。

## 阶段 7 体验细节回归修复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已在内置浏览器验证：
  - `/create/song` 显示音频上传不超过 10MB。
  - `/create/content` 称呼输入框可清空。
  - `/create/content` 显示背景图片上传不超过 3MB。
  - 选择“晨光绿”主题后，range 拖动条 computed `accent-color` 为主题绿色 `#5ba879`。
  - `/gift/demo` 音乐按钮文案为“音量”，点击后出现“音量 80%”滑块和“暂停音乐”按钮。

## 阶段 7 体验细节回归修复待办事项

- 用户用真实 3MB 以内背景图、10MB 以内音频文件在手机端做一次上传实测。
- 用户确认音量面板在不同手机安全区下不遮挡底部操作按钮。

## 阶段 7 体验细节回归修复风险 / 阻塞

- 10MB 音频会以 Data URL 进入本地草稿，部分移动端浏览器可能触发 localStorage 容量兜底；当前 `saveDraft` 会在超限时保留其他字段并移除大资源字段。
- 自包含分享链接仍不会携带上传音频或 data URL 图片，跨设备分享上传资源仍需后续 Supabase 配置。

## 阶段 7 体验细节回归修复下一步计划

- 保持当前验收服务在 `http://127.0.0.1:3000/`，请用户从首页重新走制作流程，重点验证上传限制、主题色拖动条和音量面板。

---

## 阶段 7 预览发布页流程收口今日完成

- 继续处理阶段 7 当前制作流程体验，不开启新阶段。
- 将 `/create/preview` 明确定义为最终预览与发布页，移除底部“礼物效果”跳转，避免制作者在已经能看到完整效果时再进入一层重复页面。
- 将预览页右上操作改为“祝福弹幕”开关，制作者可在最终预览中单独决定是否显示祝福弹幕。
- 新增 `blessingMarqueeEnabled` 礼物草稿字段，并接入本地草稿保存、分享链接编解码和礼物展示组件。
- 将底部主流程调整为“返回编辑 + 完成并分享”；点击完成后直接生成自包含分享链接，并提供复制链接 / 系统分享入口。
- 为弹幕开关增加轻提示，减少开关后是否生效的不确定感。

## 阶段 7 预览发布页流程收口今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已在本地验收服务和内置浏览器验证：
  - `/create/preview` 不再出现“礼物效果”入口。
  - 页面底部为“返回编辑 + 完成并分享”。
  - 弹幕开关可以隐藏 / 显示祝福弹幕，隐藏后刷新页面仍保持隐藏，说明已写入本地草稿。
  - “完成并分享”可以打开分享弹层，并提供复制链接与系统分享。
  - 本地云端保存暂不可用时，会自动降级生成 `/gift/share#bloombeat=...` 自包含链接。
  - `/`、`/create/song`、`/create/content`、`/create/preview`、`/gift/demo`、`/gift/share` 探活均返回 200。

---

## 阶段 7 预览与分享细节修复今日完成

- 继续处理阶段 7 当前制作 / 预览 / 分享链路体验，不开启新阶段。
- 背景图片上传限制从 3MB 放宽到 8MB，并同步更新页面提示和错误提示。
- 修复祝福颜色在内容页选择后与预览页不一致的问题：内容页更新草稿时同步写入 localStorage 与 IndexedDB，避免预览页优先读取旧 IndexedDB 草稿。
- 将音乐音量面板调整为竖向滑杆；当音量为 0 或音乐未播放时，按钮图标显示静音喇叭。
- 分享弹层新增“礼物名字”输入框，生成链接前可编辑礼物名称；名称会写入礼物草稿、云端保存请求和自包含分享链接。
- 修改礼物名字后会清空旧分享链接并提示重新生成，避免链接内容与当前输入不一致。

## 阶段 7 预览与分享细节修复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已在本地验收服务和内置浏览器验证：
  - `/create/content` 背景图片提示为 8MB，不再出现 3MB 文案。
  - `/create/content` 修改祝福颜色为 `#43343c` 后，`/create/preview` 弹幕 computed color 为 `rgb(67, 52, 60)`。
  - 音量面板为竖向滑杆，音量为 0 时文案为“音量 0%”，按钮进入 muted 状态并显示静音图标叉线。
  - `/create/preview` 分享弹层可输入礼物名字；输入“小满生日礼物”后生成的 `/gift/share#bloombeat=...` 链接解码 title 为“小满生日礼物”。

---

## 阶段 7 分享命名与复制反馈修复今日完成

- 继续处理阶段 7 分享弹层体验，不开启新阶段。
- 打开分享弹层时，礼物名称默认按编辑页昵称生成 `给XX的礼物`；昵称为空时回落为 `给TA的礼物`。
- 分享弹层提示用户可编辑礼物名称；用户输入时同步更新礼物草稿和预览礼品卡题目。
- 复制链接成功后，复制按钮切换为绿色成功态并显示“已复制”，增强结果反馈。

## 阶段 7 分享命名与复制反馈修复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已在本地验收服务和内置浏览器验证：
  - 编辑页昵称“小满”会作为分享弹层默认礼物名，默认值为“给小满的礼物”。
  - 打开分享弹层时，预览礼品卡题目同步为“给小满的礼物”。
  - 分享弹层修改名称为“小满的春日音乐盒”后，预览礼品卡题目同步变为“小满的春日音乐盒”，旧分享链接被清空。
  - 复制按钮已接入 `copy-done` 成功态样式；内置浏览器自动化环境实际剪贴板写入返回失败，因此本轮未能真实触发成功态截图，但代码路径会在复制成功时显示绿色“已复制”按钮。

---

## 阶段 8 云端真实分享底座今日完成

- 按产品规划先执行阶段 8 的云端分享稳定化底座，不同时展开视觉系统、天气切换或音乐 API。
- `GiftDraft` 预留未来资源和视觉扩展字段：`backgroundPresetId`、`weatherPreset`、`scenePreset`；阶段 8 不暴露天气 UI。
- 自包含分享 hash 清洗逻辑保留上述扩展字段，保证未来视觉/天气 preset 可以随礼物流转。
- `/api/upload` 云端上传限制与当前前端一致：图片 8MB、音频 10MB。
- `/api/gifts` 云端保存允许昵称为空，避免“称呼可清空”后云端保存失败；保存方式改为 `upsert`，避免同一礼物重复保存时主键冲突。
- 新增 `docs/14-stage-8-cloud-sharing.md`，记录 Supabase 环境变量、表结构、Storage bucket、上传限制、数据兼容和验收流程。
- 同步更新 `docs/00-project-overview.md`、`docs/01-prd.md`、`docs/06-api-and-data-spec.md`、`docs/08-acceptance-checklist.md`、`docs/09-risk-and-fallback.md`、`docs/11-session-handoff.md` 中过期的 15MB / 5MB 描述。

## 阶段 8 云端真实分享底座今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已协助用户配置本机 `.env.local`，使用 Supabase 项目 `https://ustqgyusndylhoovjqtn.supabase.co`。
- 已创建并验证 Storage bucket：`bloombeat-gifts`。
- 已由用户在 Supabase SQL Editor 创建 `gifts` 表，服务端可读写。
- 已验证 `/api/gifts` 可生成云端礼物 id，并修复本地草稿 id `__draft__` 被误用为云端分享 id 的问题；正式云端分享 id 现在只允许 `gift-...`，否则服务端新建 id。
- 已验证 `/create/preview` 生成正式云端链接：`/gift/gift-mquyi5v0-b446af71`，不是 `/gift/share#bloombeat=...` 兜底，也不是 `/gift/__draft__`。
- 已验证 Supabase `gifts` 表存在该礼物记录，title 为 `给HENRY的礼物`，并包含云端音频 URL。
- 已验证 Supabase Storage `audio/` 目录存在上传音频文件。
- 已验证 `/gift/gift-mquyi5v0-b446af71` 返回 200，浏览器打开后渲染礼物舞台、礼物标题、音乐按钮和分享按钮。
- 当前这份验收礼物未上传背景图，因此 Storage `image/` 目录为空属预期；后续仍需用真实图片再做一次背景图云端上传验收。

---

## 阶段 8 上传音频预览 / 发布链路修复今日完成

- 继续处理阶段 8 云端真实分享验收中暴露的单一问题：礼物预览页和生成后的礼物页没有使用用户自定义上传音乐。
- 修复 `/create/song` 音乐选择页保存策略：系统 BGM、重新选回上传音频、链接 / 推荐歌曲、首次上传音频四个入口都统一写入 `localStorage` 与 IndexedDB 草稿。
- 修复首次上传音频时只保存音乐字段的问题：现在会先合并当前完整 `GiftDraft`，再写入 `songSourceType: "upload"`、`audioUrl`，并清空 `bgmPresetId`。
- 修复 `/create/preview` 草稿合并策略：当 IndexedDB 中存在上传音频而轻量草稿缺少 `audioUrl` 时，预览页会恢复上传音频的 `songSourceType`、歌名、作者和播放 URL，不再掉回系统 BGM。

## 阶段 8 上传音频预览 / 发布链路修复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 静态代码验收确认 `GiftExperience` 仍通过 `SynthBgmButton audioUrl={gift.audioUrl} bgmPresetId={gift.bgmPresetId}` 播放音乐；当 `audioUrl` 存在时优先播放用户上传音频。
- 待用户在浏览器中完成真实文件上传验收：上传 mp3 / wav / m4a 后进入 `/create/preview`，点击音量按钮应播放上传音频；完成并分享后打开 `/gift/[id]`，点击音量按钮仍应播放同一段上传音频。

## 阶段 8 上传音频预览 / 发布链路修复风险 / 阻塞

- 浏览器自动播放策略可能阻止首次自动播放，验收时需要点击左下角“音量”按钮后再判断是否是上传音频。
- 若使用旧草稿测试，建议从首页重新开始制作一次，避免历史草稿里残留的系统 BGM 字段影响判断。

---

## 阶段 8 手势 / 触屏教程入口恢复今日完成

- 继续处理阶段 8 验收中的单一体验问题：创作者从编辑页进入最终预览、收礼人打开礼物页时缺少手势 / 触屏教程和摄像头唤起页面。
- 将 `GiftExperience` 的手势引导层恢复为首次进入默认显示，预览页和礼物页共用同一套引导、摄像头诊断、点击启动摄像头和触摸兜底说明。
- 保留页面内的 `?` 手势引导按钮与“启动摄像头”浮动按钮；用户点击“我知道了”后可以关闭教程继续互动。

## 阶段 8 手势 / 触屏教程入口恢复今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建和 TypeScript 检查通过。
- 已清理 `.next` 缓存并重启本地服务，避免旧 dev 编译结果影响验收。
- 已在内置浏览器验证 `/create/preview` 首次进入会出现 `.guide-overlay`，包含“点击启动摄像头”“回到摄像头手势”“我知道了”等入口。
- 已在内置浏览器验证 `/gift/demo` 首次进入同样出现手势引导层、摄像头启动入口和关闭按钮。

## 阶段 8 手势 / 触屏教程入口恢复风险 / 阻塞

- 手机端摄像头仍受浏览器安全策略限制：`127.0.0.1` / `localhost` 可用，局域网 HTTP 地址通常无法唤起摄像头；正式手机验收仍建议使用 HTTPS 部署或隧道。

---

## Stage 8 云端分享失败反馈补强今日完成

- 继续处理 Stage 8 云端分享与发布稳定化中的单一模块：云端保存失败时的用户反馈，不开启新阶段。
- 新增 `CloudGiftSaveError`，区分资源上传失败与礼物 JSON 保存失败，并保留 HTTP status，方便发布页给出更准确的降级原因。
- `/create/preview` 在云端保存失败后仍生成 `/gift/share#bloombeat=...` 自包含兜底链接；如果草稿中包含本地上传音频或图片，会明确提示该兜底链接无法可靠跨设备携带上传资源，需要稍后重新生成 `/gift/...` 云端链接。
- 成功路径不变：云端保存成功时仍生成 `/gift/gift-...` 链接，不影响现有主流程。

## Stage 8 云端分享失败反馈补强今日验证

- 已运行 `npm.cmd run build`，Next.js 生产构建与 TypeScript 检查通过。
- 按 `docs/08-acceptance-checklist.md` 对本模块做静态验收：
  - `/create/preview` 仍优先调用云端保存。
  - 云端失败时仍保留 hash 兜底分享能力。
  - 包含上传音频或图片时，失败提示会说明跨设备资源风险。
  - 未改动选歌、内容编辑、礼物渲染、摄像头或触摸互动主流程。

## Stage 8 云端分享失败反馈补强待办事项

- 需要在真实 Supabase 临时失败、Storage 上传失败、无云端配置三种情况下分别走一遍发布页，确认用户看到的提示与实际失败原因一致。
- 需要继续用真实图片和音频完成跨设备 Stage 8 验收，确认成功路径生成的是 `/gift/gift-...` 而不是兜底 hash 链接。

## Stage 8 云端分享失败反馈补强风险 / 阻塞

- 当前本地构建可验证类型与成功路径不被破坏，但没有主动破坏 Supabase 配置做失败态浏览器实测。
- hash 兜底链接仍无法可靠携带本地上传的大音频或 data URL 图片，这一限制本次只是显式提示，根本解决仍依赖云端 Storage 成功。

## Stage 8 云端分享失败反馈补强下一步计划

- 下一步优先做 Stage 8 真实图片 + 真实音频的端到端云端分享验收；稳定后再进入 HTTPS/Vercel 部署和移动端跨浏览器验证。

---

## Stage 8 图片与音频云端链路 API 验收今日完成

- 继续执行 Stage 8 云端分享稳定化的下一步，只做图片 + 音频云端链路验收，不进入视觉、天气、音乐 API 或手势调参。
- 使用程序生成极小 PNG 图片和 0.25 秒 WAV 音频，模拟真实上传资源。
- 通过本地服务 `/api/upload` 上传图片和音频到 Supabase Storage。
- 通过 `/api/gifts` 保存包含云端图片 URL 与云端音频 URL 的礼物数据，生成正式云端礼物 id：`gift-mqv2cxp0-1f4166bd`。
- 更新 `docs/15-stage-8-handoff.md`，记录新的 API 级图片 + 音频验收礼物和资源路径。

## Stage 8 图片与音频云端链路 API 验收今日验证

- 本地服务 `http://127.0.0.1:3000/` 返回 200。
- 图片上传成功，返回 Supabase Storage URL：
  `https://ustqgyusndylhoovjqtn.supabase.co/storage/v1/object/public/bloombeat-gifts/image/mqv2cvz2-c028fcd5-18ae-45b1-b0f6-f3884de15587.png`
- 音频上传成功，返回 Supabase Storage URL：
  `https://ustqgyusndylhoovjqtn.supabase.co/storage/v1/object/public/bloombeat-gifts/audio/mqv2cxfc-a5131dfa-1db2-4e40-9a16-a9f923d36866.wav`
- `/api/gifts` 保存成功，返回 `/gift/gift-mqv2cxp0-1f4166bd`。
- `/api/gifts/gift-mqv2cxp0-1f4166bd` 读取成功，title、audioUrl、backgroundImageUrl 与保存内容一致。
- `/gift/gift-mqv2cxp0-1f4166bd` 返回 200。
- 内置浏览器打开 `/gift/gift-mqv2cxp0-1f4166bd` 后加载态消失，页面渲染出 `Stage 8 验证礼物`、祝福文案、主题舞台、手势引导层、音量入口和转发入口。
- 页面背景 style 中使用 Supabase Storage 图片 URL，证明云端背景图已进入礼物渲染路径。
- 点击“我知道了”可关闭引导层；点击“音量”可打开音量面板并显示“暂停音乐”。当前 `SynthBgmButton` 通过 `new Audio(audioUrl)` 播放上传音频，不会在 DOM 中挂载 `<audio>` 标签，因此 DOM 中看不到音频 URL 属于预期实现。

## Stage 8 图片与音频云端链路 API 验收待办事项

- 继续做浏览器 UI 级验收：从 `/create/song` 真实选择 mp3 / wav / m4a，再到 `/create/content` 真实选择图片，最后在 `/create/preview` 点击完成并分享。
- 使用另一浏览器或移动设备打开生成的 `/gift/gift-...`，确认图片、音频、标题、祝福、主题和弹幕开关完整一致。
- 在 WeChat、iPhone Safari、Android Chrome 和桌面 Chrome 中分别验证链接打开、复制和系统分享行为。

## Stage 8 图片与音频云端链路 API 验收风险 / 阻塞

- 本次是 API 级验收，证明 Supabase Storage 和 Database 链路可用，但还没有覆盖浏览器文件选择器、移动端内置浏览器音频策略和真实设备播放体验。
- 内置浏览器验收无法操作系统文件选择器，因此还不能替代从 `/create/song` 和 `/create/content` 真实选择本地文件的完整前端上传流程。
- 本次生成了 Supabase 验收文件和礼物记录，当前项目还没有清理测试资源的后台工具；后续如需要可补一个管理员清理流程或手动在 Supabase 中删除。

## Stage 8 图片与音频云端链路 API 验收下一步计划

- 下一步继续 Stage 8 浏览器 UI 级端到端验收，重点验证从用户真实上传文件到生成 `/gift/gift-...` 的完整前端路径。

---

## Stage 8 浏览器 UI 上传验收包今日完成

- 继续执行 Stage 8 浏览器 UI 级端到端验收准备，只做验收辅助资源，不进入新阶段。
- 新增本地验收目录 `stage8-validation-assets/`。
- 生成可直接用于文件选择器的测试资源：
  - `stage8-ui-audio.wav`：约 64KB，2 秒 WAV 音频。
  - `stage8-ui-background.png`：约 244B，PNG 背景图。
- 新增 `stage8-validation-assets/README.md`，写明如何验收、验收什么、验收成功后继续执行什么。
- 更新 `docs/15-stage-8-handoff.md`，把浏览器 UI 验收文件位置和下一步验收路径写入交接文档。

## Stage 8 浏览器 UI 上传验收包今日验证

- 已确认 `stage8-validation-assets/stage8-ui-audio.wav` 存在且大小约 64KB，低于 10MB 音频上传限制。
- 已确认 `stage8-validation-assets/stage8-ui-background.png` 存在且大小约 244B，低于 8MB 图片上传限制。
- 本地服务此前已确认 `http://127.0.0.1:3000/` 可访问，下一步可直接从首页开始真实上传验收。

## Stage 8 浏览器 UI 上传验收包待办事项

- 使用 `stage8-validation-assets/stage8-ui-audio.wav` 从 `/create/song` 真实选择上传音频。
- 使用 `stage8-validation-assets/stage8-ui-background.png` 从 `/create/content` 真实选择上传图片。
- 在 `/create/preview` 生成分享链接，确认生成的是 `/gift/gift-...`，并在另一个浏览器或设备打开。

## Stage 8 浏览器 UI 上传验收包风险 / 阻塞

- 当前自动化浏览器接口无法可靠操作系统文件选择器，因此这一步必须由用户在本机浏览器中手动选择文件。
- 验收资源是小型测试文件，只验证上传链路和跨设备云端资源持久化，不代表最终视觉质量或音乐体验。

## Stage 8 浏览器 UI 上传验收包下一步计划

- 用户按 `stage8-validation-assets/README.md` 走完真实 UI 上传流程后，继续记录生成的 `/gift/gift-...` 链接，并进入另一浏览器 / 手机端打开验证。

---

## Stage 8 UI 图片上传验收问题定位今日完成

- 用户反馈：上传图片没有进入内容页预览框，也没有作为背景生成；同时提供生成链接 `http://127.0.0.1:3000/gift/gift-mqv3reol-bd4160d6`。
- 读取 `/api/gifts/gift-mqv3reol-bd4160d6` 后确认：该云端礼物已经包含 Supabase `backgroundImageUrl`，说明发布保存链路拿到了图片 URL。
- 校验该 `backgroundImageUrl`：Supabase 图片 URL 返回 200，Content-Type 为 `image/png`。
- 检查本地验收素材时发现原 `stage8-ui-background.png` 只有 244B，且本地图片查看工具无法解析，不适合作为肉眼判断预览/背景是否生效的验收图。
- 已重新生成 `stage8-validation-assets/stage8-ui-background.png`，新图约 116KB，包含明显的 `BloomBeat / Stage 8 UI background` 字样和粉绿背景，便于确认内容页预览框与最终礼物背景是否切换成功。
- 已更新 `stage8-validation-assets/README.md` 和 `docs/15-stage-8-handoff.md`。

## Stage 8 UI 图片上传验收问题定位今日验证

- 新 `stage8-ui-background.png` 已通过本地图片查看，能正常解析并显示明显测试图案。
- 用户提供的云端礼物 `/gift/gift-mqv3reol-bd4160d6` 返回 200。
- 云端礼物数据中 `audioUrl` 和 `backgroundImageUrl` 都是 Supabase Storage URL，说明本轮问题优先按验收素材不可见 / 不可解析处理。

## Stage 8 UI 图片上传验收问题定位待办事项

- 用户需要用重新生成的 `stage8-validation-assets/stage8-ui-background.png` 再走一次 `/create/content` 图片上传。
- 上传后应立即看到内容页预览框切换为带有 `BloomBeat / Stage 8 UI background` 字样的粉绿背景。
- 如果新图仍不进入预览框，则下一步进入代码修复：重点检查 `handleImageUpload`、`compressImage`、`saveDraft` / `saveLocalDraft` 和 `GiftBackground` 的即时状态更新。

## Stage 8 UI 图片上传验收问题定位风险 / 阻塞

- 当前无法直接代替用户操作系统文件选择器，因此还不能完全确认内容页手动上传行为是否仍失败。
- 用户提供的旧链接已经保存了背景 URL，但旧验收图片本身不可见 / 不可解析，可能造成“已保存但看起来没变化”的误判。

## Stage 8 UI 图片上传验收问题定位下一步计划

- 先让用户使用新背景图重试 UI 上传验收；若仍失败，再修改内容页上传状态与预览渲染逻辑。
